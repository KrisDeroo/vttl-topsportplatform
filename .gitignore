.DS_Store
node_modules/
.next/
.turbo/
.env
.env.local
.env.*.local
*.log

# Test + build artifacts
coverage/
playwright-report/
test-results/
.vitest-cache/

# Generated test fixtures (built by tests/fixtures/build-*.ts before test runs).
# EICAR is documented in tests/fixtures/README.md — do not commit the
# generated artefact to avoid AV-scanner false positives at git push time
# (T-02-15-FIXTURE-LEAK mitigation).
tests/fixtures/eicar.png

# IDE
.vscode/
.idea/

# OS
Thumbs.db

# Note: drizzle/meta/_journal.json is NOT gitignored — must be committed
# (Drizzle Kit consults it to generate the next migration in sequence)
tsconfig.tsbuildinfo
