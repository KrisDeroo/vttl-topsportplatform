---
phase: 03-kalender
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - messages/nl.json
  - messages/en.json
  - messages/fr.json
  - src/app/[locale]/globals.css
autonomous: true
requirements:
  - CAL-03
  - CAL-05
  - CAL-07
  - I18N-05
  - I18N-06
  - I18N-07
  - I18N-08
threat_refs:
  - V1
  - V8
must_haves:
  truths:
    - "All Phase 3 user-facing strings exist in all 3 locales (nl/en/fr) under namespaces `calendar.*`, `lookup.eventType.*`, `errors.calendar.*` per UI-SPEC §Copywriting Contract"
    - "D-57b conflict copy override applied verbatim in all 3 locales (participant-first template with `**{participant}** is al geboekt voor {detail} {start}–{end}. Toch opslaan?` and 2 localized equivalents)"
    - "D-58b delete copy override applied verbatim in all 3 locales (no 30-day-restore promise: `Deze afspraak wordt definitief verwijderd voor alle deelnemers.` and 2 localized equivalents)"
    - "globals.css adds 6 event-type color token triples × light + dark = 36 token declarations + FullCalendar `--fc-*` variable overrides + mobile breakpoint --fc-event-min-height override"
    - "All 6 event_type lookup labels present under `lookup.eventType.*` in all 3 locales per UI-SPEC §Lookup additions"
    - "FullCalendar locale strings (`weekday names, month names, time format) remain owned by `@fullcalendar/core/locales/{nl,en-gb,fr}` packages — NOT duplicated in messages catalogs (UI-SPEC declared exception)"
  artifacts:
    - path: "messages/nl.json"
      provides: "Phase 3 nl strings including D-57b + D-58b overrides"
      contains: "calendar"
    - path: "messages/en.json"
      provides: "Phase 3 en strings"
      contains: "calendar"
    - path: "messages/fr.json"
      provides: "Phase 3 fr strings"
      contains: "calendar"
    - path: "src/app/[locale]/globals.css"
      provides: "6 event-type token triples × 2 modes + FullCalendar variable overrides + mobile @media block"
      contains: "--cal-event-training-bg"
  key_links:
    - from: "messages/nl.json"
      to: "calendar.conflict.body"
      via: "D-57b participant-first template"
      pattern: "is al geboekt voor"
    - from: "messages/nl.json"
      to: "calendar.event.delete.body"
      via: "D-58b no-restore copy"
      pattern: "definitief verwijderd"
    - from: "src/app/[locale]/globals.css"
      to: "FullCalendar --fc-* tokens"
      via: ".fc selector block"
      pattern: "--fc-border-color"
---

<objective>
Land every user-facing string and every CSS design token Phase 3 needs in a single coordinated edit: 3 locale message catalogs (nl/en/fr) extended with `calendar.*` + `lookup.eventType.*` + `errors.calendar.*` namespaces per UI-SPEC §Copywriting Contract, and `globals.css` extended with the 6 event-type color token triples × light+dark + FullCalendar variable overrides per UI-SPEC §Color and §FullCalendar overrides.

Apply the two CONTEXT.md copy overrides directly here — D-57b (participant-first conflict body) and D-58b (no-30-day-restore delete body) — so the UI components in Plan 06/07 consume the canonical text without needing a UI-SPEC revision.

Purpose: Catalogs and tokens are required by every UI surface (Plan 06/07) and every Zod error path (Plan 03 schemas already reference the i18n keys). Shipping them in a dedicated parallel-to-Plan-03 wave avoids blocking Plan 03 on i18n drafting and avoids blocking UI plans on catalog readiness.

Output: 3 modified message catalogs + 1 modified globals.css. Tests `tests/unit/color-tokens.test.ts` and `tests/unit/schema-locale.test.ts` (already RED scaffolds) can flip to GREEN after this plan lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-kalender/03-UI-SPEC.md
@.planning/phases/03-kalender/03-CONTEXT.md
@.planning/phases/03-kalender/03-RESEARCH.md
@.planning/phases/03-kalender/03-PATTERNS.md
@messages/nl.json
@messages/en.json
@messages/fr.json
@src/app/[locale]/globals.css

<interfaces>
<!-- The canonical UI-SPEC Copywriting Contract source rows + D-57b/D-58b overrides. -->

D-57b conflict body — overrides UI-SPEC line 653 `calendar.conflict.body`:
- nl: `**{participant}** is al geboekt voor {detail} {start}–{end}. Toch opslaan?`
- en: `**{participant}** is already booked for {detail} {start}–{end}. Save anyway?`
- fr: `**{participant}** est déjà réservé pour {detail} {start}–{end}. Enregistrer quand même ?`

D-58b delete body — overrides UI-SPEC line 665 `calendar.event.delete.body`:
- nl: `Deze afspraak wordt definitief verwijderd voor alle deelnemers.`
- en: `This event will be permanently deleted for all participants.`
- fr: `Ce rendez-vous sera supprimé définitivement pour tous les participants.`

The 6 event_type lookup labels (UI-SPEC line 524-535):
| Code | nl | en | fr |
|------|----|----|----|
| event_type_training | Training | Training | Entraînement |
| event_type_tournament | Toernooi | Tournament | Tournoi |
| event_type_meeting | Vergadering | Meeting | Réunion |
| event_type_stage | Stage | Camp | Stage |
| event_type_eval_conversation | Evaluatiegesprek | Evaluation conversation | Entretien d'évaluation |
| event_type_medical | Medische afspraak | Medical appointment | Rendez-vous médical |

Existing `messages/nl.json` top-level keys (verified): `auth`, `consent`, `lookups`, `errors`. Append `calendar`, `lookup`, and EXTEND `errors`.

NOTE: existing top-level is `lookups` (plural — Phase 1/2 keys); UI-SPEC §Lookup additions specifies `lookup.eventType.*` (singular). This is a small drift; we will add the new keys under `lookup.eventType` (singular new namespace) so the contract surface in UI-SPEC matches our import. The existing `lookups.*` namespace remains untouched.

globals.css structure (existing): Tailwind v4 CSS-first; `:root { ... }` and `.dark { ... }` blocks for color tokens. Append the 6 event-type token triples × 2 modes below existing tokens. Append `.fc { ... --fc-border-color: var(--border); ... }` for FullCalendar overrides. Append `@media (max-width: 640px) { .fc { --fc-event-min-height: 2.75rem; } }` for WCAG 2.5.5 mobile tap-target rule.

The full FullCalendar override list per UI-SPEC §FullCalendar built-in CSS-variable overrides (lines 219-250):
```css
.fc {
  --fc-border-color: var(--border);
  --fc-page-bg-color: var(--background);
  --fc-neutral-bg-color: var(--muted);
  --fc-neutral-text-color: var(--muted-foreground);
  --fc-today-bg-color: color-mix(in oklch, var(--primary) 6%, transparent);
  --fc-button-bg-color: var(--secondary);
  --fc-button-text-color: var(--secondary-foreground);
  --fc-button-border-color: var(--border);
  --fc-button-active-bg-color: var(--primary);
  --fc-button-active-text-color: var(--primary-foreground);
  --fc-button-active-border-color: var(--primary);
  --fc-button-hover-bg-color: var(--accent);
  --fc-button-hover-text-color: var(--accent-foreground);
  --fc-event-text-color: var(--foreground);
  --fc-event-border-color: var(--border);
  --fc-event-selected-overlay-color: color-mix(in oklch, var(--primary) 12%, transparent);
  --fc-now-indicator-color: var(--primary);
  --fc-small-font-size: 0.75rem;
  --fc-event-min-height: 1.5rem;
}

@media (max-width: 640px) {
  .fc {
    --fc-event-min-height: 2.75rem;
  }
}
```

The 6 event-type token triples × 2 modes per UI-SPEC §Color (lines 146-202) — copy verbatim into globals.css; oklch() values are LOCKED (not negotiable):

Light mode (in :root):
```css
--cal-event-training-bg:       oklch(0.96 0.04 250);
--cal-event-training-fg:       oklch(0.35 0.15 250);
--cal-event-training-border:   oklch(0.55 0.18 250);
--cal-event-tournament-bg:     oklch(0.96 0.05 60);
--cal-event-tournament-fg:     oklch(0.40 0.18 50);
--cal-event-tournament-border: oklch(0.65 0.20 55);
--cal-event-meeting-bg:        oklch(0.96 0.04 145);
--cal-event-meeting-fg:        oklch(0.38 0.13 145);
--cal-event-meeting-border:    oklch(0.55 0.16 145);
--cal-event-stage-bg:          oklch(0.96 0.04 295);
--cal-event-stage-fg:          oklch(0.38 0.15 295);
--cal-event-stage-border:      oklch(0.55 0.18 295);
--cal-event-evalconv-bg:       oklch(0.96 0.05 90);
--cal-event-evalconv-fg:       oklch(0.40 0.12 85);
--cal-event-evalconv-border:   oklch(0.70 0.16 90);
--cal-event-medical-bg:        oklch(0.96 0.05 25);
--cal-event-medical-fg:        oklch(0.38 0.16 25);
--cal-event-medical-border:    oklch(0.55 0.20 25);
```

Dark mode (in .dark):
```css
--cal-event-training-bg:       oklch(0.25 0.04 250);
--cal-event-training-fg:       oklch(0.85 0.08 250);
--cal-event-training-border:   oklch(0.60 0.15 250);
--cal-event-tournament-bg:     oklch(0.25 0.05 50);
--cal-event-tournament-fg:     oklch(0.85 0.10 60);
--cal-event-tournament-border: oklch(0.65 0.18 55);
--cal-event-meeting-bg:        oklch(0.25 0.04 145);
--cal-event-meeting-fg:        oklch(0.85 0.08 145);
--cal-event-meeting-border:    oklch(0.60 0.14 145);
--cal-event-stage-bg:          oklch(0.25 0.04 295);
--cal-event-stage-fg:          oklch(0.85 0.09 295);
--cal-event-stage-border:      oklch(0.60 0.15 295);
--cal-event-evalconv-bg:       oklch(0.25 0.05 85);
--cal-event-evalconv-fg:       oklch(0.85 0.10 90);
--cal-event-evalconv-border:   oklch(0.70 0.15 90);
--cal-event-medical-bg:        oklch(0.25 0.05 25);
--cal-event-medical-fg:        oklch(0.85 0.10 25);
--cal-event-medical-border:    oklch(0.65 0.18 25);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend messages/{nl,en,fr}.json with Phase 3 catalog namespaces + D-57b + D-58b overrides</name>
  <files>messages/nl.json, messages/en.json, messages/fr.json</files>
  <read_first>
    - messages/nl.json (full file — observe existing JSON structure; preserve top-level key order)
    - messages/en.json (full file)
    - messages/fr.json (full file)
    - 03-UI-SPEC.md §Copywriting Contract (lines 558-700) — full table of canonical keys + nl/en/fr text
    - 03-UI-SPEC.md §Localization Contract (lines 512-535) — lookup.eventType labels
    - 03-CONTEXT.md D-57b (conflict body override — verbatim text for 3 locales)
    - 03-CONTEXT.md D-58b (delete body override — verbatim text for 3 locales)
    - 03-PATTERNS.md §`messages/{nl,en,fr}.json` (lines 993-1069) — JSON shape to append
  </read_first>
  <action>
    Open each of `messages/nl.json`, `messages/en.json`, `messages/fr.json` and APPEND the new namespaces. Do NOT rewrite existing keys; preserve the top-level structure (`auth`, `consent`, `lookups`, `errors` namespaces stay where they are).

    For each file, add THREE top-level entries: `calendar`, `lookup` (NEW singular namespace — distinct from existing `lookups`), and MERGE into the existing `errors` block a new `calendar` sub-namespace.

    **For `messages/nl.json` — add these entries (preserve JSON structure; do not break existing keys):**

    Append to top-level (between `lookups` and `errors`, or wherever JSON object insertion is cleanest):

    ```json
    "calendar": {
      "title": "Kalender",
      "loading": "Agenda wordt geladen…",
      "views": {
        "week": "Week",
        "day": "Dag",
        "month": "Maand",
        "year": "Jaar"
      },
      "actions": {
        "today": "Vandaag",
        "prev": "Vorige",
        "next": "Volgende",
        "create": "Nieuwe afspraak",
        "filters": "Filters"
      },
      "filters": {
        "byType": "Type",
        "byPlayer": "Speler",
        "byTrainer": "Trainer",
        "bySparring": "Sparring partner",
        "byAcademy": "Academie",
        "searchPlaceholder": "Zoeken…",
        "empty": "Geen resultaten in jouw scope",
        "clear": "Filters wissen",
        "apply": "Toepassen",
        "countSuffix": " · {count}"
      },
      "emptyHint": "Geen afspraken in deze periode. Voeg er een toe of navigeer naar een andere week.",
      "emptyFiltered": "Geen afspraken bij deze filters. Pas de filters aan of wis ze.",
      "event": {
        "untitled": "Naamloze afspraak",
        "sections": {
          "when": "Wanneer",
          "where": "Waar",
          "who": "Wie",
          "description": "Beschrijving",
          "recurrence": "Herhaling"
        },
        "recurrence": {
          "weekly": "Wekelijks tot {endDate}",
          "daily": "Dagelijks tot {endDate}",
          "monthly": "Maandelijks tot {endDate}",
          "noEnd": "Geen einddatum",
          "frequency": "Frequentie",
          "interval": "Elke {n} {unit}",
          "endLabel": "Eindigt",
          "endNever": "Nooit",
          "endOnDate": "Op datum",
          "endAfter": "Na {n} keer",
          "scopeThis": "Deze afspraak",
          "scopeFuture": "Deze en toekomstige (Fase 4)",
          "scopeAll": "Alle afspraken in de reeks (Fase 4)"
        },
        "fields": {
          "title": "Titel",
          "type": "Type",
          "start": "Start",
          "end": "Einde",
          "allDay": "Hele dag",
          "location": "Locatie",
          "description": "Beschrijving",
          "recurring": "Herhalend",
          "participants": "Deelnemers"
        },
        "actions": {
          "edit": "Afspraak bewerken",
          "delete": "Afspraak verwijderen",
          "close": "Sluiten",
          "cancel": "Annuleren"
        },
        "create": {
          "title": "Nieuwe afspraak",
          "submit": "Afspraak aanmaken"
        },
        "edit": {
          "title": "Afspraak wijzigen",
          "submit": "Wijzigingen opslaan"
        },
        "delete": {
          "title": "Afspraak verwijderen?",
          "body": "Deze afspraak wordt definitief verwijderd voor alle deelnemers.",
          "confirm": "Verwijderen",
          "cancel": "Niet verwijderen"
        },
        "decline": {
          "title": "Ik kan niet aanwezig zijn",
          "submit": "Bevestigen",
          "toast": "Je deelname is gewijzigd"
        },
        "toast": {
          "created": "Afspraak aangemaakt",
          "updated": "Wijzigingen opgeslagen",
          "deleted": "Afspraak verwijderd",
          "error": "Opslaan mislukt — controleer de gegevens en probeer opnieuw",
          "notFound": "Afspraak niet gevonden",
          "moved": "Afspraak verplaatst",
          "resized": "Tijden aangepast"
        }
      },
      "conflict": {
        "title": "Conflicteert met bestaande afspraak",
        "body": "**{participant}** is al geboekt voor {detail} {start}–{end}. Toch opslaan?",
        "extra": "+{n} andere conflicten",
        "saveAnyway": "Toch opslaan",
        "adjustTime": "Tijden aanpassen",
        "moveAnyway": "Toch verplaatsen",
        "undoMove": "Ongedaan maken",
        "detailFull": "**{title}** ({typeLabel})",
        "detailRedacted": "een **{typeLabel}**"
      }
    },
    "lookup": {
      "eventType": {
        "event_type_training": "Training",
        "event_type_tournament": "Toernooi",
        "event_type_meeting": "Vergadering",
        "event_type_stage": "Stage",
        "event_type_eval_conversation": "Evaluatiegesprek",
        "event_type_medical": "Medische afspraak"
      }
    }
    ```

    Also EXTEND the existing `errors` block by adding a `calendar` sub-entry:

    ```json
    "errors": {
      // ... existing nl error keys ...
      "calendar": {
        "endBeforeStart": "Einde moet na de start liggen",
        "rruleHorizonExceeded": "Herhaling kan maximaal 2 jaar vooruit lopen",
        "rangeTooLarge": "Een afspraak kan maximaal 24 uur duren in v1",
        "titleRequired": "Een titel is verplicht",
        "typeRequired": "Een type is verplicht",
        "medicalPastStart": "Medische afspraken kunnen niet in het verleden worden aangemaakt",
        "participantNotInScope": "Eén of meer geselecteerde deelnemers vallen buiten je bereik"
      }
    }
    ```

    **For `messages/en.json` — same structure, en text per UI-SPEC §Copywriting Contract:**

    ```json
    "calendar": {
      "title": "Calendar",
      "loading": "Loading calendar…",
      "views": { "week": "Week", "day": "Day", "month": "Month", "year": "Year" },
      "actions": {
        "today": "Today",
        "prev": "Previous",
        "next": "Next",
        "create": "New event",
        "filters": "Filters"
      },
      "filters": {
        "byType": "Type",
        "byPlayer": "Player",
        "byTrainer": "Trainer",
        "bySparring": "Sparring partner",
        "byAcademy": "Academy",
        "searchPlaceholder": "Search…",
        "empty": "No results in your scope",
        "clear": "Clear filters",
        "apply": "Apply filters",
        "countSuffix": " · {count}"
      },
      "emptyHint": "No events in this range. Add one or navigate to another week.",
      "emptyFiltered": "No events match these filters. Adjust them or clear all.",
      "event": {
        "untitled": "Untitled event",
        "sections": {
          "when": "When",
          "where": "Where",
          "who": "Who",
          "description": "Description",
          "recurrence": "Recurrence"
        },
        "recurrence": {
          "weekly": "Weekly until {endDate}",
          "daily": "Daily until {endDate}",
          "monthly": "Monthly until {endDate}",
          "noEnd": "No end date",
          "frequency": "Frequency",
          "interval": "Every {n} {unit}",
          "endLabel": "Ends",
          "endNever": "Never",
          "endOnDate": "On date",
          "endAfter": "After {n} times",
          "scopeThis": "This event",
          "scopeFuture": "This and future events (Phase 4)",
          "scopeAll": "All events in the series (Phase 4)"
        },
        "fields": {
          "title": "Title",
          "type": "Type",
          "start": "Start",
          "end": "End",
          "allDay": "All day",
          "location": "Location",
          "description": "Description",
          "recurring": "Recurring",
          "participants": "Participants"
        },
        "actions": {
          "edit": "Edit event",
          "delete": "Delete event",
          "close": "Close",
          "cancel": "Discard changes"
        },
        "create": { "title": "New event", "submit": "Create event" },
        "edit": { "title": "Edit event", "submit": "Save changes" },
        "delete": {
          "title": "Delete event?",
          "body": "This event will be permanently deleted for all participants.",
          "confirm": "Delete event",
          "cancel": "Keep event"
        },
        "decline": {
          "title": "I cannot attend",
          "submit": "Confirm",
          "toast": "Your participation has been updated"
        },
        "toast": {
          "created": "Event created",
          "updated": "Changes saved",
          "deleted": "Event deleted",
          "error": "Save failed — review the data and try again",
          "notFound": "Event not found",
          "moved": "Event moved",
          "resized": "Times updated"
        }
      },
      "conflict": {
        "title": "Conflicts with existing event",
        "body": "**{participant}** is already booked for {detail} {start}–{end}. Save anyway?",
        "extra": "+{n} other conflicts",
        "saveAnyway": "Save anyway",
        "adjustTime": "Adjust times",
        "moveAnyway": "Move anyway",
        "undoMove": "Undo",
        "detailFull": "**{title}** ({typeLabel})",
        "detailRedacted": "a **{typeLabel}**"
      }
    },
    "lookup": {
      "eventType": {
        "event_type_training": "Training",
        "event_type_tournament": "Tournament",
        "event_type_meeting": "Meeting",
        "event_type_stage": "Camp",
        "event_type_eval_conversation": "Evaluation conversation",
        "event_type_medical": "Medical appointment"
      }
    }
    ```

    Extend `errors`:
    ```json
    "errors": {
      // ... existing en error keys ...
      "calendar": {
        "endBeforeStart": "End must be after start",
        "rruleHorizonExceeded": "Recurrence can extend at most 2 years ahead",
        "rangeTooLarge": "An event can last up to 24 hours in v1",
        "titleRequired": "A title is required",
        "typeRequired": "A type is required",
        "medicalPastStart": "Medical events cannot be created in the past",
        "participantNotInScope": "One or more selected participants are outside your scope"
      }
    }
    ```

    **For `messages/fr.json` — same structure, fr text per UI-SPEC §Copywriting Contract:**

    ```json
    "calendar": {
      "title": "Calendrier",
      "loading": "Chargement de l'agenda…",
      "views": { "week": "Semaine", "day": "Jour", "month": "Mois", "year": "Année" },
      "actions": {
        "today": "Aujourd'hui",
        "prev": "Précédent",
        "next": "Suivant",
        "create": "Nouveau rendez-vous",
        "filters": "Filtres"
      },
      "filters": {
        "byType": "Type",
        "byPlayer": "Joueur",
        "byTrainer": "Entraîneur",
        "bySparring": "Partenaire de sparring",
        "byAcademy": "Académie",
        "searchPlaceholder": "Rechercher…",
        "empty": "Aucun résultat dans votre périmètre",
        "clear": "Effacer les filtres",
        "apply": "Appliquer",
        "countSuffix": " · {count}"
      },
      "emptyHint": "Aucun rendez-vous sur cette période. Ajoutez-en un ou naviguez vers une autre semaine.",
      "emptyFiltered": "Aucun rendez-vous ne correspond à ces filtres. Ajustez-les ou effacez-les.",
      "event": {
        "untitled": "Rendez-vous sans titre",
        "sections": {
          "when": "Quand",
          "where": "Où",
          "who": "Qui",
          "description": "Description",
          "recurrence": "Récurrence"
        },
        "recurrence": {
          "weekly": "Toutes les semaines jusqu'au {endDate}",
          "daily": "Tous les jours jusqu'au {endDate}",
          "monthly": "Tous les mois jusqu'au {endDate}",
          "noEnd": "Pas de date de fin",
          "frequency": "Fréquence",
          "interval": "Tous les {n} {unit}",
          "endLabel": "Se termine",
          "endNever": "Jamais",
          "endOnDate": "À la date",
          "endAfter": "Après {n} fois",
          "scopeThis": "Cet événement",
          "scopeFuture": "Cet événement et les suivants (Phase 4)",
          "scopeAll": "Tous les événements de la série (Phase 4)"
        },
        "fields": {
          "title": "Titre",
          "type": "Type",
          "start": "Début",
          "end": "Fin",
          "allDay": "Toute la journée",
          "location": "Lieu",
          "description": "Description",
          "recurring": "Récurrent",
          "participants": "Participants"
        },
        "actions": {
          "edit": "Modifier le rendez-vous",
          "delete": "Supprimer le rendez-vous",
          "close": "Fermer",
          "cancel": "Annuler les modifications"
        },
        "create": { "title": "Nouveau rendez-vous", "submit": "Créer le rendez-vous" },
        "edit": { "title": "Modifier le rendez-vous", "submit": "Enregistrer les modifications" },
        "delete": {
          "title": "Supprimer le rendez-vous ?",
          "body": "Ce rendez-vous sera supprimé définitivement pour tous les participants.",
          "confirm": "Supprimer le rendez-vous",
          "cancel": "Conserver"
        },
        "decline": {
          "title": "Je ne peux pas être présent",
          "submit": "Confirmer",
          "toast": "Votre participation a été mise à jour"
        },
        "toast": {
          "created": "Rendez-vous créé",
          "updated": "Modifications enregistrées",
          "deleted": "Rendez-vous supprimé",
          "error": "Échec de l'enregistrement — vérifiez les données et réessayez",
          "notFound": "Rendez-vous introuvable",
          "moved": "Rendez-vous déplacé",
          "resized": "Horaires modifiés"
        }
      },
      "conflict": {
        "title": "Conflit avec un rendez-vous existant",
        "body": "**{participant}** est déjà réservé pour {detail} {start}–{end}. Enregistrer quand même ?",
        "extra": "+{n} autres conflits",
        "saveAnyway": "Enregistrer quand même",
        "adjustTime": "Ajuster les horaires",
        "moveAnyway": "Déplacer quand même",
        "undoMove": "Annuler",
        "detailFull": "**{title}** ({typeLabel})",
        "detailRedacted": "un **{typeLabel}**"
      }
    },
    "lookup": {
      "eventType": {
        "event_type_training": "Entraînement",
        "event_type_tournament": "Tournoi",
        "event_type_meeting": "Réunion",
        "event_type_stage": "Stage",
        "event_type_eval_conversation": "Entretien d'évaluation",
        "event_type_medical": "Rendez-vous médical"
      }
    }
    ```

    Extend `errors`:
    ```json
    "errors": {
      // ... existing fr error keys ...
      "calendar": {
        "endBeforeStart": "La fin doit être après le début",
        "rruleHorizonExceeded": "La récurrence peut s'étendre au maximum sur 2 ans",
        "rangeTooLarge": "Un rendez-vous peut durer 24 heures maximum en v1",
        "titleRequired": "Un titre est obligatoire",
        "typeRequired": "Un type est obligatoire",
        "medicalPastStart": "Les rendez-vous médicaux ne peuvent pas être créés dans le passé",
        "participantNotInScope": "Un ou plusieurs participants sélectionnés sont hors de votre périmètre"
      }
    }
    ```

    After editing all three JSON files, run `pnpm test -- schema-locale` to confirm catalog drift detection finds no missing keys across the 3 locales.

    Validate JSON syntax: each file must parse cleanly. Use `node -e "JSON.parse(require('fs').readFileSync('messages/nl.json','utf8'))"` style checks (or trust your IDE).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('messages/nl.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('messages/en.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('messages/fr.json','utf8'))" && grep -q '"calendar":' messages/nl.json && grep -q '"calendar":' messages/en.json && grep -q '"calendar":' messages/fr.json && grep -q '"lookup":' messages/nl.json && grep -q '"event_type_training":' messages/nl.json && grep -q '"event_type_training":' messages/en.json && grep -q '"event_type_training":' messages/fr.json && grep -q 'is al geboekt voor' messages/nl.json && grep -q 'is already booked for' messages/en.json && grep -q 'est déjà réservé pour' messages/fr.json && grep -q 'definitief verwijderd' messages/nl.json && grep -q 'permanently deleted' messages/en.json && grep -q 'définitivement' messages/fr.json && ! grep -q 'omkeerbaar binnen 30 dagen' messages/nl.json && ! grep -q 'reversible within 30 days' messages/en.json && ! grep -q 'réversible dans les 30 jours' messages/fr.json && grep -q 'rruleHorizonExceeded' messages/nl.json && grep -q 'endBeforeStart' messages/nl.json</automated>
  </verify>
  <done>All 3 JSON files parse; D-57b conflict body present in 3 locales with `**{participant}**` token; D-58b delete body present and the 30-day-restore text is NOT present anywhere; 6 event_type lookup labels and 7 errors.calendar keys present in all 3 locales.</done>
</task>

<task type="auto">
  <name>Task 2: Extend src/app/[locale]/globals.css with 6 event-type tokens × 2 modes + FullCalendar overrides</name>
  <files>src/app/[locale]/globals.css</files>
  <read_first>
    - src/app/[locale]/globals.css (full file — observe existing `:root { ... }` and `.dark { ... }` blocks; append below them, do not edit existing tokens)
    - 03-UI-SPEC.md §Color (lines 114-215) + §FullCalendar built-in CSS-variable overrides (lines 219-250) — verbatim source for both blocks
    - 03-PATTERNS.md §Design tokens (CSS) (lines 84-88)
    - 03-UI-SPEC.md §Spacing Scale (lines 71-77) — declared exception `--fc-event-min-height: 2.75rem` at <640px
  </read_first>
  <action>
    Open `src/app/[locale]/globals.css`. Identify the existing `:root { ... }` block (Phase 1+2 chrome tokens) and the existing `.dark { ... }` block. Add the new Phase 3 tokens INSIDE these existing blocks (do not create a second `:root { ... }` block — append within the existing one between the closing `}` of the last existing token group and the existing `}` that closes the block).

    **Step A:** Inside `:root { ... }`, append the 6 event-type light-mode token triples (oklch values verbatim per UI-SPEC, locked):

    ```css
      /* Phase 3 — event-type color tokens (UI-SPEC §Color). LOCKED. */
      --cal-event-training-bg:       oklch(0.96 0.04 250); /* blue tint */
      --cal-event-training-fg:       oklch(0.35 0.15 250); /* deep blue */
      --cal-event-training-border:   oklch(0.55 0.18 250); /* mid blue */

      --cal-event-tournament-bg:     oklch(0.96 0.05 60);  /* orange tint */
      --cal-event-tournament-fg:     oklch(0.40 0.18 50);  /* deep orange */
      --cal-event-tournament-border: oklch(0.65 0.20 55);  /* mid orange */

      --cal-event-meeting-bg:        oklch(0.96 0.04 145); /* green tint */
      --cal-event-meeting-fg:        oklch(0.38 0.13 145); /* deep green */
      --cal-event-meeting-border:    oklch(0.55 0.16 145); /* mid green */

      --cal-event-stage-bg:          oklch(0.96 0.04 295); /* purple tint */
      --cal-event-stage-fg:          oklch(0.38 0.15 295); /* deep purple */
      --cal-event-stage-border:      oklch(0.55 0.18 295); /* mid purple */

      --cal-event-evalconv-bg:       oklch(0.96 0.05 90);  /* yellow tint */
      --cal-event-evalconv-fg:       oklch(0.40 0.12 85);  /* deep amber */
      --cal-event-evalconv-border:   oklch(0.70 0.16 90);  /* mid yellow */

      --cal-event-medical-bg:        oklch(0.96 0.05 25);  /* red tint */
      --cal-event-medical-fg:        oklch(0.38 0.16 25);  /* deep red */
      --cal-event-medical-border:    oklch(0.55 0.20 25);  /* mid red */
    ```

    **Step B:** Inside `.dark { ... }`, append the 6 event-type dark-mode token triples:

    ```css
      /* Phase 3 — event-type color tokens (UI-SPEC §Color). LOCKED. */
      --cal-event-training-bg:       oklch(0.25 0.04 250);
      --cal-event-training-fg:       oklch(0.85 0.08 250);
      --cal-event-training-border:   oklch(0.60 0.15 250);

      --cal-event-tournament-bg:     oklch(0.25 0.05 50);
      --cal-event-tournament-fg:     oklch(0.85 0.10 60);
      --cal-event-tournament-border: oklch(0.65 0.18 55);

      --cal-event-meeting-bg:        oklch(0.25 0.04 145);
      --cal-event-meeting-fg:        oklch(0.85 0.08 145);
      --cal-event-meeting-border:    oklch(0.60 0.14 145);

      --cal-event-stage-bg:          oklch(0.25 0.04 295);
      --cal-event-stage-fg:          oklch(0.85 0.09 295);
      --cal-event-stage-border:      oklch(0.60 0.15 295);

      --cal-event-evalconv-bg:       oklch(0.25 0.05 85);
      --cal-event-evalconv-fg:       oklch(0.85 0.10 90);
      --cal-event-evalconv-border:   oklch(0.70 0.15 90);

      --cal-event-medical-bg:        oklch(0.25 0.05 25);
      --cal-event-medical-fg:        oklch(0.85 0.10 25);
      --cal-event-medical-border:    oklch(0.65 0.18 25);
    ```

    **Step C:** After both blocks, append a new top-level FullCalendar override block + mobile media query (do NOT nest inside any selector — `.fc` is a separate root selector that FullCalendar root-element renders with):

    ```css

    /* Phase 3 — FullCalendar v6 CSS-variable overrides (UI-SPEC §FullCalendar built-in CSS-variable overrides) */
    .fc {
      --fc-border-color: var(--border);
      --fc-page-bg-color: var(--background);
      --fc-neutral-bg-color: var(--muted);
      --fc-neutral-text-color: var(--muted-foreground);
      --fc-today-bg-color: color-mix(in oklch, var(--primary) 6%, transparent);
      --fc-button-bg-color: var(--secondary);
      --fc-button-text-color: var(--secondary-foreground);
      --fc-button-border-color: var(--border);
      --fc-button-active-bg-color: var(--primary);
      --fc-button-active-text-color: var(--primary-foreground);
      --fc-button-active-border-color: var(--primary);
      --fc-button-hover-bg-color: var(--accent);
      --fc-button-hover-text-color: var(--accent-foreground);
      --fc-event-text-color: var(--foreground);
      --fc-event-border-color: var(--border);
      --fc-event-selected-overlay-color: color-mix(in oklch, var(--primary) 12%, transparent);
      --fc-now-indicator-color: var(--primary);
      --fc-small-font-size: 0.75rem;     /* time-axis labels at 12px — declared exception per UI-SPEC §Typography */
      --fc-event-min-height: 1.5rem;     /* desktop default */
    }

    @media (max-width: 640px) {
      .fc {
        --fc-event-min-height: 2.75rem;  /* 44px — WCAG 2.5.5 mobile tap target — UI-SPEC §Spacing exceptions */
      }
    }
    ```

    **Optional but recommended (Tailwind v4 inline @theme exposure):**

    If the existing `globals.css` uses Tailwind v4's `@theme inline { ... }` block to expose CSS variables as Tailwind utility classes (e.g. `bg-background` → `--background`), append the 18 new token names to that block so components can use `bg-cal-event-training-bg`, `text-cal-event-training-fg`, etc. If the existing file doesn't use `@theme inline` (Phase 1/2 chose another exposure path), components can use the raw `var(--cal-event-training-bg)` inline-style approach — that works with Tailwind v4 arbitrary value syntax (`bg-[oklch(...)]` is overkill; raw CSS via inline style is cleanest).

    Run `pnpm build` to confirm Tailwind v4 rebuilds cleanly (no CSS parse errors).
  </action>
  <verify>
    <automated>grep -c "cal-event-training-bg\|cal-event-tournament-bg\|cal-event-meeting-bg\|cal-event-stage-bg\|cal-event-evalconv-bg\|cal-event-medical-bg" src/app/[locale]/globals.css | awk '{exit ($1 >= 12 ? 0 : 1)}' && grep -c "cal-event-training-fg\|cal-event-tournament-fg\|cal-event-meeting-fg\|cal-event-stage-fg\|cal-event-evalconv-fg\|cal-event-medical-fg" src/app/[locale]/globals.css | awk '{exit ($1 >= 12 ? 0 : 1)}' && grep -c "cal-event-training-border\|cal-event-tournament-border\|cal-event-meeting-border\|cal-event-stage-border\|cal-event-evalconv-border\|cal-event-medical-border" src/app/[locale]/globals.css | awk '{exit ($1 >= 12 ? 0 : 1)}' && grep -q '\-\-fc-border-color: var(\-\-border)' src/app/[locale]/globals.css && grep -q '\-\-fc-page-bg-color: var(\-\-background)' src/app/[locale]/globals.css && grep -q '\-\-fc-small-font-size: 0\.75rem' src/app/[locale]/globals.css && grep -q '@media (max-width: 640px)' src/app/[locale]/globals.css && grep -q '\-\-fc-event-min-height: 2\.75rem' src/app/[locale]/globals.css && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <done>18 light-mode + 18 dark-mode `--cal-event-*` token declarations present; FullCalendar variable overrides present; mobile media query overrides `--fc-event-min-height` to 2.75rem; `pnpm build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-facing string → rendered UI | next-intl `useTranslations` does the interpolation; markdown-style `**bold**` is rendered safely by our markdown helper (no XSS); placeholder substitution is escape-safe |
| CSS variable → browser paint | oklch() is widely supported in Chromium 111+, Safari 16.4+, Firefox 113+; older browsers degrade to default chrome tokens (no medical privacy implication — chip is still distinguishable via icon glyph per UI-SPEC §A11y) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17-I18N-DRIFT | I, T | messages/{nl,en,fr}.json key coverage | mitigate | Wave 0 test `tests/unit/schema-locale.test.ts` (extended in Plan 01) enforces all-keys-present-in-all-locales. CI run on Plan 04 PR catches drift; Phase 8 I18N-10 release gate is the final backstop. |
| T-03-18-COPY-OVERRIDE-MISAPPLIED | V11 | D-57b + D-58b override texts | mitigate | Plan 04 ships the canonical copy directly; the old UI-SPEC text ("omkeerbaar binnen 30 dagen") is grep-asserted absent. UI-SPEC revision is out-of-band per CONTEXT and not a blocker. |
| T-03-19-CSS-ESCAPE-VIA-OKLCH | V8 | globals.css oklch() values | accept | Values are static literal numbers (no user input); copied verbatim from UI-SPEC LOCKED contract. No injection vector. |
| T-03-20-DARK-MODE-CONTRAST | V8 | --cal-event-{type}-{bg,fg} dark mode | mitigate | UI-SPEC declared L=0.85 fg vs L=0.25 bg → delta 0.6 > AA 0.55 threshold. Manual visual review listed in 03-VALIDATION.md §Manual-Only Verifications row "Color contrast" — design sign-off in Wave 5. |
</threat_model>

<verification>
- All 3 JSON files parse cleanly with `node -e JSON.parse(...)`.
- D-57b participant-first conflict body present in all 3 locales; old "Conflicteert met **{title}** op {start} – {end}" removed (or never re-added).
- D-58b no-restore delete body present; the 30-day-restore promise text is absent from all 3 locales.
- 6 event_type lookup labels present in all 3 locales.
- 7 errors.calendar.* keys present in all 3 locales.
- 18 light + 18 dark cal-event tokens present in globals.css.
- FullCalendar variable overrides present.
- Mobile @media min-height override present.
- `pnpm build` succeeds (Tailwind v4 + PostCSS).
</verification>

<success_criteria>
- 3 message catalog files extended (NOT rewritten) with `calendar`, `lookup` namespaces and extended `errors.calendar.*`.
- `globals.css` extended (NOT rewritten) with 18 light + 18 dark tokens + FullCalendar overrides + mobile @media block.
- `pnpm test -- schema-locale` passes (catalog drift detector).
- `pnpm build` succeeds.
- `tests/unit/color-tokens.test.ts` can now flip from RED to GREEN when its `it()` blocks are activated in Wave 5.
</success_criteria>

<output>
After completion, create `.planning/phases/03-kalender/03-04-SUMMARY.md` documenting:
- The 3 catalog files modified with the appended namespace sizes (calendar.* ≈ 60 keys, lookup.eventType.* = 6 keys, errors.calendar.* = 7 keys)
- The 36 new `--cal-event-*` token declarations + 19 FullCalendar `--fc-*` overrides + 1 mobile @media override
- Confirmation D-57b + D-58b copy applied verbatim
- Confirmation old UI-SPEC line 665 30-day-restore promise removed (or never appeared in catalogs)
</output>
