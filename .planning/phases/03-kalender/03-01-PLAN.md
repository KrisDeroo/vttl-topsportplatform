---
phase: 03-kalender
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - components.json
  - src/components/ui/sheet.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/command.tsx
  - src/components/ui/toggle.tsx
  - src/components/ui/toggle-group.tsx
  - src/components/ui/scroll-area.tsx
  - tests/unit/rrule.test.ts
  - tests/unit/color-tokens.test.ts
  - tests/unit/calendar-schemas.test.ts
  - tests/integration/calendar-rls.test.ts
  - tests/integration/calendar-rrule-horizon.test.ts
  - tests/integration/calendar-exceptions.test.ts
  - tests/integration/calendar-conflicts.test.ts
  - tests/integration/calendar-audit.test.ts
  - tests/integration/calendar-cascade.test.ts
  - tests/integration/calendar-decline.test.ts
  - tests/integration/calendar-perf.test.ts
  - tests/integration/calendar-filter-options.test.ts
  - tests/rls/calendar-direct-query.test.ts
  - tests/e2e/calendar-week-view.spec.ts
  - tests/e2e/calendar-create-event.spec.ts
  - tests/e2e/calendar-mobile.spec.ts
  - tests/e2e/calendar-drag.spec.ts
  - tests/fixtures/calendar-seed.ts
  - tests/unit/lookup-codes.test.ts
  - tests/unit/schema-locale.test.ts
  - tests/unit/migration-format.test.ts
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
  - CAL-05
  - CAL-07
  - CAL-08
  - TRAIN-01
  - TOURN-01
  - MED-EVENT
  - GDPR-04
  - GDPR-08
  - I18N-05
  - I18N-06
  - I18N-07
  - I18N-08
  - USER-04
threat_refs:
  - V1
  - V5
must_haves:
  truths:
    - "FullCalendar 6.1.20 + rrule 2.8.1 + 6 new shadcn primitives are installed and importable"
    - "20 Wave 0 RED test files exist with describe() blocks asserting Phase 3 behaviour (running them = expected failures)"
    - "Shared `seedCalendarFixtures` helper exists and exports a TD + 2 trainers + 6 players + academy + 1 event per type fixture"
  artifacts:
    - path: "package.json"
      provides: "FullCalendar v6 + rrule deps in dependencies"
      contains: "@fullcalendar/core"
    - path: "src/components/ui/sheet.tsx"
      provides: "shadcn Sheet primitive"
    - path: "src/components/ui/alert.tsx"
      provides: "shadcn Alert primitive"
    - path: "src/components/ui/command.tsx"
      provides: "shadcn Command primitive (typeahead)"
    - path: "src/components/ui/toggle.tsx"
      provides: "shadcn Toggle primitive"
    - path: "src/components/ui/toggle-group.tsx"
      provides: "shadcn ToggleGroup primitive"
    - path: "src/components/ui/scroll-area.tsx"
      provides: "shadcn ScrollArea primitive"
    - path: "tests/unit/rrule.test.ts"
      provides: "RRULE pure-function unit test (RED)"
      contains: "expandRrule"
    - path: "tests/fixtures/calendar-seed.ts"
      provides: "Shared seed helper for calendar tests"
      contains: "seedCalendarFixtures"
  key_links:
    - from: "package.json"
      to: "@fullcalendar/core@^6.1.20"
      via: "dependencies"
      pattern: "\"@fullcalendar/core\""
    - from: "package.json"
      to: "rrule@^2.8.1"
      via: "dependencies"
      pattern: "\"rrule\""
---

<objective>
Install all Phase 3 NPM dependencies (FullCalendar 6.1.20 + 7 plugins + rrule 2.8.1) and shadcn primitives (sheet, alert, command, toggle, toggle-group, scroll-area), then scaffold the 20 Wave 0 RED test files declared in `03-VALIDATION.md` + the shared `calendar-seed.ts` fixture. Establishes Nyquist-compliant verify hooks for every Wave 1+ task.

Purpose: Wave 0 must precede schema/library/tRPC implementation so every later task has a passing-or-failing automated verify command. Per D-55, D-57, D-58c, RRULE horizon + conflict redaction + audit-snapshot tests must exist before the code that satisfies them lands.

Output: Installed dependencies + 6 new shadcn components + 20 test files with `describe()` blocks asserting the canonical Phase 3 behaviour (per `03-VALIDATION.md` Wave 0 requirements).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kalender/03-CONTEXT.md
@.planning/phases/03-kalender/03-RESEARCH.md
@.planning/phases/03-kalender/03-VALIDATION.md
@.planning/phases/03-kalender/03-PATTERNS.md
@.planning/phases/03-kalender/03-UI-SPEC.md
@tests/helpers/seed.ts
@tests/helpers/db.ts
@tests/integration/rbac-matrix.test.ts
@tests/rls/players-direct-query.test.ts
@tests/unit/migration-format.test.ts
@tests/unit/schema-locale.test.ts
@tests/unit/lookup-codes.test.ts

<interfaces>
<!-- Test infrastructure already shipped in Phase 1/2 — Wave 0 tests reuse these. -->

From tests/helpers/db.ts:
```typescript
export async function freshDb(): Promise<{ db, sql, [Symbol.asyncDispose] }>;
export async function rawPgAsAppUser<T>(args: {
  userId: string;
  role: string;
  sql: string;
  params?: unknown[];
}): Promise<T[]>;
```

From tests/helpers/seed.ts:
```typescript
export const ROLES: readonly ['technical_director','academy_manager','trainer','player','parent','sparring_partner','medical_staff'];
export async function seedRolesMatrix(db): Promise<{ users: Record<Role, string>; victimId: string }>;
```

From tests/helpers/trpc.ts:
```typescript
export function appCaller(args: { userId: string; role: string }): AppCaller;
```

shadcn primitive add command (single line, all 6 in one run):
```bash
npx shadcn@latest add sheet alert command toggle toggle-group scroll-area
```

NPM install (single line):
```bash
pnpm add @fullcalendar/core@^6.1.20 @fullcalendar/react@^6.1.20 @fullcalendar/timegrid@^6.1.20 @fullcalendar/daygrid@^6.1.20 @fullcalendar/interaction@^6.1.20 @fullcalendar/rrule@^6.1.20 @fullcalendar/multimonth@^6.1.20 rrule@^2.8.1
```

The 6 event type codes Wave 0 tests reference (canonical per UI3-D11 + D-47):
- event_type_training
- event_type_tournament
- event_type_meeting
- event_type_stage
- event_type_eval_conversation
- event_type_medical
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install FullCalendar + rrule npm dependencies and shadcn primitives</name>
  <files>package.json, pnpm-lock.yaml, components.json, src/components/ui/sheet.tsx, src/components/ui/alert.tsx, src/components/ui/command.tsx, src/components/ui/toggle.tsx, src/components/ui/toggle-group.tsx, src/components/ui/scroll-area.tsx</files>
  <read_first>
    - package.json (verify current deps, scripts, pnpm version 9.15.0)
    - components.json (verify shadcn config: style=new-york, baseColor=neutral, rsc=true, iconLibrary=lucide)
    - src/components/ui/dialog.tsx (existing primitive — analog for the new ones)
    - src/components/ui/calendar.tsx (existing shadcn date-picker, NOT FullCalendar — confirm no naming clash before adding Sheet)
    - 03-PATTERNS.md §Components to add (lines 38-51) + 03-RESEARCH.md §Standard Stack (lines 158-194)
  </read_first>
  <action>
    Run two install commands sequentially (the second depends on the first finishing):

    1. NPM install (FullCalendar v6 plugin set + rrule):
    ```bash
    pnpm add @fullcalendar/core@^6.1.20 @fullcalendar/react@^6.1.20 @fullcalendar/timegrid@^6.1.20 @fullcalendar/daygrid@^6.1.20 @fullcalendar/interaction@^6.1.20 @fullcalendar/rrule@^6.1.20 @fullcalendar/multimonth@^6.1.20 rrule@^2.8.1
    ```

    2. shadcn add (all 6 primitives in one CLI invocation — per UI-SPEC §Components to add):
    ```bash
    pnpm dlx shadcn@latest add sheet alert command toggle toggle-group scroll-area --yes --overwrite
    ```
    (If `--yes --overwrite` flags cause interactive prompts in this shadcn version, fall back to running each component on its own line without flags; do not abort on a single component already present — overwrite is safe because these are net-new for Phase 3 per UI-SPEC line 53 "Already-shipped" list.)

    After both commands run, verify:
    - `package.json` `dependencies` contains all 8 new npm packages (7 FullCalendar + rrule).
    - 6 new files exist under `src/components/ui/` matching the shadcn add list.
    - `components.json` is unchanged (config inherited from Phase 1).

    Do NOT yet write any usage code — that lands in Wave 4. This task is dependency setup only.

    Per D-52: rrule 2.8.1 is the canonical RRULE library; `@fullcalendar/rrule` is installed but NOT wired (server-side expansion is the rule per D-53; the FC plugin is installed only to keep the option open without re-architecture cost).
  </action>
  <verify>
    <automated>grep -q "@fullcalendar/core" package.json && grep -q "@fullcalendar/react" package.json && grep -q "@fullcalendar/timegrid" package.json && grep -q "@fullcalendar/daygrid" package.json && grep -q "@fullcalendar/interaction" package.json && grep -q "@fullcalendar/rrule" package.json && grep -q "@fullcalendar/multimonth" package.json && grep -q "\"rrule\"" package.json && test -f src/components/ui/sheet.tsx && test -f src/components/ui/alert.tsx && test -f src/components/ui/command.tsx && test -f src/components/ui/toggle.tsx && test -f src/components/ui/toggle-group.tsx && test -f src/components/ui/scroll-area.tsx && pnpm typecheck</automated>
  </verify>
  <done>All 8 npm deps + 6 shadcn primitives present; `pnpm typecheck` passes (no usage code yet, only declarations and ui primitives).</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold 20 Wave 0 RED test files + shared seed fixture</name>
  <files>tests/unit/rrule.test.ts, tests/unit/color-tokens.test.ts, tests/unit/calendar-schemas.test.ts, tests/integration/calendar-rls.test.ts, tests/integration/calendar-rrule-horizon.test.ts, tests/integration/calendar-exceptions.test.ts, tests/integration/calendar-conflicts.test.ts, tests/integration/calendar-audit.test.ts, tests/integration/calendar-cascade.test.ts, tests/integration/calendar-decline.test.ts, tests/integration/calendar-perf.test.ts, tests/integration/calendar-filter-options.test.ts, tests/rls/calendar-direct-query.test.ts, tests/e2e/calendar-week-view.spec.ts, tests/e2e/calendar-create-event.spec.ts, tests/e2e/calendar-mobile.spec.ts, tests/e2e/calendar-drag.spec.ts, tests/fixtures/calendar-seed.ts, tests/unit/lookup-codes.test.ts, tests/unit/schema-locale.test.ts, tests/unit/migration-format.test.ts</files>
  <read_first>
    - 03-VALIDATION.md (full file — the canonical 20-file Wave 0 manifest)
    - 03-PATTERNS.md §Test files — Wave 0 (lines 89-110) and §`tests/unit/rrule.test.ts` (lines 1085-1107) and §`tests/integration/calendar-rls.test.ts` (lines 1107-1135) and §`tests/integration/calendar-audit.test.ts` (lines 1136-1148) and §`tests/rls/calendar-direct-query.test.ts` (lines 1150-1158) and §`tests/fixtures/calendar-seed.ts` (lines 1159-1175) and §`tests/e2e/calendar-mobile.spec.ts` (lines 1178-1199)
    - tests/integration/rbac-matrix.test.ts (canonical analog for calendar-rls.test.ts)
    - tests/integration/medical-audit.test.ts (canonical analog for calendar-audit.test.ts)
    - tests/integration/age-category-history.test.ts (canonical analog for calendar-exceptions.test.ts — sequential INSERT/close-old-row/INSERT)
    - tests/integration/medical-delete.test.ts (canonical analog for calendar-cascade.test.ts — assertion on row absence post-DELETE)
    - tests/integration/consent.test.ts (canonical analog for calendar-decline.test.ts — mutation flipping status on caller's own row)
    - tests/integration/admin-user.test.ts (canonical analog for calendar-filter-options.test.ts — TD admin list with role-scoping)
    - tests/integration/player-router.test.ts (canonical analog for calendar-rrule-horizon.test.ts — TRPCError i18n-key message assertions)
    - tests/rls/players-direct-query.test.ts (canonical analog for calendar-direct-query.test.ts — `rawPgAsAppUser` + GUC-bound vs unbound probes)
    - tests/e2e/photo-upload.spec.ts (canonical Playwright shape for the 4 e2e specs)
    - tests/unit/players-derive-age-category.test.ts (pure-function with date inputs — rrule.test.ts analog)
    - tests/unit/migration-format.test.ts (current state — Wave 0 must EXTEND, not rewrite)
    - tests/unit/lookup-codes.test.ts + tests/unit/schema-locale.test.ts (current state — EXTEND)
    - tests/helpers/seed.ts (lines 1-100 — seedRolesMatrix pattern; we wrap on top of it in calendar-seed.ts)
    - tests/helpers/db.ts (freshDb + rawPgAsAppUser signatures)
    - 03-CONTEXT.md (D-50 sparring-partner no-op assertion; D-54 exception cancel/move/retitle; D-55 horizon; D-56/D-57 cross-scope redaction; D-58c JSONB snapshot)
  </read_first>
  <action>
    Create the **20 RED test files + 1 fixture** declared in `03-VALIDATION.md` Wave 0 Requirements. Each file must have:
    - Vitest `describe()` + `it()` structure (or Playwright `test()` for e2e).
    - `it.todo()` or `it()` with assertions that WILL fail today (no Wave 1+ implementation yet — that is the Nyquist RED contract).
    - Top-of-file JSDoc with `Reference: 03-CONTEXT.md D-XX, 03-RESEARCH.md §X, 03-VALIDATION.md` linking the test to its source decision.

    **File-by-file canonical scaffolding (all fields are concrete — copy verbatim):**

    ---

    `tests/unit/rrule.test.ts` (new) — pure-function RRULE coverage per D-52, D-54, D-55, RESEARCH §Pattern 3 + §Pitfall 3:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { expandRrule, validateHorizon, parseRrule } from '@/lib/rrule';

    describe('expandRrule — RFC 5545 expansion (D-53)', () => {
      it.todo('DST boundary: weekly Tuesday 10:00 Europe/Brussels — Oct 25 stays at 10:00 local');
      it.todo('applies exceptions: cancelled occurrences are skipped');
      it.todo('applies overrides: overrideStartsAt + overrideEndsAt replace original');
      it.todo('clamps read-time at dtstart + 2y per D-55');
    });

    describe('validateHorizon — D-55 write-time gate', () => {
      it.todo('rejects RRULE without UNTIL or COUNT');
      it.todo('rejects UNTIL beyond createdAt + 2y');
      it.todo('accepts UNTIL within window');
      it.todo('rejects rrule string containing DTSTART: per RESEARCH Anti-Pattern 1');
    });

    describe('parseRrule', () => {
      it.todo('throws on invalid RFC 5545 string');
      it.todo('parses FREQ=WEEKLY;UNTIL=... with explicit dtstart');
    });
    ```

    ---

    `tests/unit/color-tokens.test.ts` (new) — assert 18 cal-event tokens × 2 modes in globals.css per UI-SPEC §Color (lines 146-202):
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { readFileSync } from 'node:fs';
    import { join } from 'node:path';

    const css = readFileSync(join(process.cwd(), 'src/app/[locale]/globals.css'), 'utf8');

    describe('Phase 3 design tokens — UI-SPEC §Color', () => {
      const TYPES = ['training','tournament','meeting','stage','evalconv','medical'] as const;
      const SLOTS = ['bg','fg','border'] as const;

      it.each(TYPES)('declares --cal-event-%s-{bg,fg,border} in :root', (type) => {
        for (const slot of SLOTS) {
          expect(css).toContain(`--cal-event-${type}-${slot}:`);
        }
      });

      it.each(TYPES)('declares --cal-event-%s-{bg,fg,border} in .dark', (type) => {
        // crude scope detection: ensure both :root and .dark blocks each contain the token name
        const darkBlock = css.split('.dark {')[1]?.split('}')[0] ?? '';
        for (const slot of SLOTS) {
          expect(darkBlock).toContain(`--cal-event-${type}-${slot}:`);
        }
      });

      it.todo('FullCalendar variable overrides --fc-border-color / --fc-page-bg-color present in .fc block');
      it.todo('Mobile @media (max-width: 640px) overrides --fc-event-min-height: 2.75rem');
    });
    ```

    ---

    `tests/unit/calendar-schemas.test.ts` (new) — Zod discriminated-union per RESEARCH §Pattern 5:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { eventCreateInput } from '@/server/trpc/schemas/calendar';

    describe('eventCreateInput — discriminated union per event type', () => {
      it.todo('accepts a valid training payload (type=event_type_training + TRAIN-01 fields)');
      it.todo('accepts a valid tournament payload (type=event_type_tournament + TOURN-01 fields)');
      it.todo('accepts a valid medical payload (type=event_type_medical + MED-EVENT fields)');
      it.todo('rejects training payload with unknown field (.strict() — VALID-06)');
      it.todo('emits i18n-key error messages (errors.calendar.endBeforeStart / errors.field.required)');
      it.todo('rejects when endsAt <= startsAt');
    });
    ```

    ---

    `tests/integration/calendar-rls.test.ts` (new) — 30-case sample of 5 roles × 6 event types per D-50 + USER-04:
    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { freshDb } from '../helpers/db';
    import { seedRolesMatrix } from '../helpers/seed';
    import { seedCalendarFixtures } from '../fixtures/calendar-seed';
    import { appCaller } from '../helpers/trpc';

    const ROLES = ['technical_director','academy_manager','trainer','player','parent','sparring_partner'] as const;
    const EVENT_TYPES = ['event_type_training','event_type_tournament','event_type_meeting','event_type_stage','event_type_eval_conversation','event_type_medical'] as const;

    describe('calendar RLS — 5 roles × 6 event types (D-50, USER-04)', () => {
      let dbHandle: Awaited<ReturnType<typeof freshDb>>;
      beforeAll(async () => { dbHandle = await freshDb(); });
      afterAll(async () => { await dbHandle[Symbol.asyncDispose](); });

      describe.each(ROLES)('role: %s', (role) => {
        describe.each(EVENT_TYPES)('event type: %s', (type) => {
          it.todo(`returns the expected scope via tRPC list`);
          it.todo(`returns the expected scope via direct psql as app_user`);
        });
      });

      // D-50 explicit no-op for sparring_partner in Phase 3
      it.todo('sparring_partner sees ZERO events in Phase 3 (D-50 no-op until Phase 4 SPAR table)');
    });
    ```

    ---

    `tests/integration/calendar-rrule-horizon.test.ts` (new) — D-55 defense in depth:
    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { freshDb } from '../helpers/db';

    describe('Calendar RRULE 2-year horizon — D-55 defense in depth', () => {
      it.todo('write-time: rejects calendar.event.create with UNTIL beyond +2y → errors.calendar.rruleHorizonExceeded');
      it.todo('write-time: rejects rrule without UNTIL or COUNT → errors.calendar.rruleHorizonExceeded');
      it.todo('write-time: auto-injects UNTIL=createdAt+2y when "Eindigt: Nooit" is sent via ensureHorizon()');
      it.todo('read-time: calendar.list({from,to}) rejects (to - from) > 2y → errors.calendar.rangeTooLarge');
      it.todo('read-time: legacy row with UNTIL+5y is clamped to startsAt+2y on expansion (no exception thrown)');
    });
    ```

    ---

    `tests/integration/calendar-exceptions.test.ts` (new) — D-54 single-occurrence override:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('calendar_event_exceptions — D-54 single-occurrence override', () => {
      it.todo('cancel: writing cancelled=true skips the occurrence in expansion');
      it.todo('move: overrideStartsAt + overrideEndsAt replace the original times');
      it.todo('retitle: overrideTitle replaces title for that occurrence only');
      it.todo('UNIQUE(event_id, occurrence_date) prevents duplicate exception rows');
      it.todo('FK CASCADE on calendar_events delete drops associated exceptions');
    });
    ```

    ---

    `tests/integration/calendar-conflicts.test.ts` (new) — D-56 per-participant + D-57 cross-scope + redaction:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('Conflict detection — D-56 per-participant overlap', () => {
      it.todo('same time + same participant in both events → conflict detected');
      it.todo('same time + different participants → no conflict');
      it.todo('recurring event B in candidate event A range → expansion ±15d catches conflict');
      it.todo('no location-based conflict (D-56 explicit — free-text location is not a conflict surface)');
    });

    describe('Conflict cross-scope detection — D-57', () => {
      it.todo('SECURITY DEFINER overlapping_events_for_users() bypasses RLS');
      it.todo('caller=academy_manager sees medical conflict for academy player (cross-scope correctness)');
      it.todo('service layer applies role-gated redaction: detailMode=full when caller IS participant');
      it.todo('service layer applies role-gated redaction: detailMode=redacted when caller is NOT participant and NOT TD');
      it.todo('redacted response omits title + location + description; only type-code + participant-name + time-range present');
      it.todo('TD always gets detailMode=full regardless');
    });

    describe('Conflict override audit — D-57', () => {
      it.todo('force:true flag on calendar.event.create writes calendar_event_conflict_override audit row');
      it.todo('blocked:false in every conflict response (D-57: soft warning, never block)');
    });
    ```

    ---

    `tests/integration/calendar-audit.test.ts` (new) — 6 audit action codes per GDPR-04 + D-58c:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('Calendar audit_log — 6 action codes (GDPR-04)', () => {
      it.todo('calendar_event_created — audit row contains typeCode and new participant ids');
      it.todo('calendar_event_updated — audit row contains old + new state diff');
      it.todo('calendar_event_deleted — JSONB snapshot includes base + extension + participants + exceptions (D-58c)');
      it.todo('calendar_event_deleted — snapshot cap: exceptions array <= 1000 with exceptionsTotalCount when truncated');
      it.todo('calendar_event_declined — RSVP decline writes per-caller audit row');
      it.todo('calendar_event_conflict_override — force-save writes override audit row BEFORE the mutation');
      it.todo('calendar_event_exception_created — single-occurrence override writes audit row');
    });
    ```

    ---

    `tests/integration/calendar-cascade.test.ts` (new) — FK CASCADE on delete per D-58:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('calendar_events DELETE — FK CASCADE per D-58', () => {
      it.todo('DELETE FROM calendar_events drops matching training_sessions row');
      it.todo('DELETE FROM calendar_events drops all calendar_event_participants rows');
      it.todo('DELETE FROM calendar_events drops all calendar_event_exceptions rows');
      it.todo('AlertDialog confirmation is mandatory (UI test) — verified via e2e');
      it.todo('NO deleted_at column on calendar_events (hard delete only, audit-log is the forensic recovery path)');
    });
    ```

    ---

    `tests/integration/calendar-decline.test.ts` (new) — RSVP decline ≠ delete per D-58:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('calendar.event.declineParticipation — D-58 RSVP decline', () => {
      it.todo('declineParticipation by participant updates own row to rsvp_status=declined');
      it.todo('event still visible to other participants after decline');
      it.todo('mutation row-WHERE clause prevents declining another user (RSVP forgery prevention)');
      it.todo('UI: declined event renders strikethrough + 50% opacity (test asserts CSS class via DOM probe)');
    });
    ```

    ---

    `tests/integration/calendar-perf.test.ts` (new) — RISK-POLYMORPH < 200ms p95:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('Calendar performance — RISK-POLYMORPH', () => {
      it.todo('calendar.list with 200 events + 30 RRULE rows over a week range completes in p95 < 200ms');
    });
    ```

    ---

    `tests/integration/calendar-filter-options.test.ts` (new) — scope-filtered type-ahead per CAL-04:
    ```typescript
    import { describe, it, expect } from 'vitest';

    describe('calendar.filterOptions.list — scope-filtered type-ahead', () => {
      it.todo('trainer-as-caller for kind=player returns only own-academy players');
      it.todo('player-as-caller for kind=player returns at most themselves');
      it.todo('sparring_partner-as-caller for kind=player returns empty array');
      it.todo('TD-as-caller for kind=academy returns ALL academy lookups');
      it.todo('respects 50-option max + 200ms debounce contract (debounce is client-side; server enforces limit)');
    });
    ```

    ---

    `tests/rls/calendar-direct-query.test.ts` (new) — direct psql RLS proof per USER-04:
    ```typescript
    import { describe, it, expect, beforeAll, afterAll } from 'vitest';
    import { sql } from 'drizzle-orm';
    import { freshDb, rawPgAsAppUser } from '../helpers/db';

    describe('USER-04 RLS direct DB query — calendar_events', () => {
      it.todo('trainer-as-participant sees only events they participate in or created');
      it.todo('trainer-non-participant sees 0 rows for non-academy events');
      it.todo('no GUC bound → 0 rows (default-deny baseline)');
      it.todo('sparring_partner sees 0 rows in Phase 3 (D-50 no-op)');
    });
    ```

    ---

    e2e specs (`tests/e2e/calendar-week-view.spec.ts`, `tests/e2e/calendar-create-event.spec.ts`, `tests/e2e/calendar-mobile.spec.ts`, `tests/e2e/calendar-drag.spec.ts`) — each with `test.skip(true, 'RED scaffold — implementation lands in Wave 4')` blocks plus a `test.todo()` for each ROADMAP succescriterium. Use the photo-upload.spec.ts shape for the Playwright bootstrap.

    `tests/e2e/calendar-week-view.spec.ts` content:
    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Calendar — week view (CAL-01, CAL-03)', () => {
      test.todo('TD login → /nl/calendar loads, week view is the default, 6 event-type colors are visible');
      test.todo('clicking an event opens EventDetailSheet with title + type chip + time range');
      test.todo('Month + Year view buttons in toolbar switch the visible view (CAL-02)');
    });
    ```

    `tests/e2e/calendar-create-event.spec.ts` content:
    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Calendar — drag-create + conflict warning (CAL-07)', () => {
      test.todo('drag-select range on grid → EventCreateSheet opens with start/end pre-filled');
      test.todo('overlapping participant → ConflictWarning surfaces with participant-name copy');
      test.todo('clicking "Toch opslaan" succeeds and audit_log gets calendar_event_conflict_override row');
    });
    ```

    `tests/e2e/calendar-mobile.spec.ts` content (UI-SPEC §Mobile Strategy):
    ```typescript
    import { test, expect } from '@playwright/test';

    test.use({ viewport: { width: 360, height: 640 } });

    test.describe('Calendar — mobile single-day + swipe (CAL-08)', () => {
      test.todo('< 640px viewport forces timeGridDay; week/year buttons hidden');
      test.todo('horizontal swipe (60px x-delta) calls FullCalendar API .next() / .prev()');
      test.todo('"Nieuwe afspraak" CTA floats bottom-right via fixed positioning');
    });
    ```

    `tests/e2e/calendar-drag.spec.ts` content:
    ```typescript
    import { test, expect } from '@playwright/test';

    test.describe('Calendar — drag-to-edit + optimistic revert', () => {
      test.todo('drag event to new time → optimistic move + tRPC update succeeds → toast.moved');
      test.todo('drag event to conflicting time → revert + ConflictBanner appears');
    });
    ```

    ---

    `tests/fixtures/calendar-seed.ts` (new) — analog: `tests/helpers/seed.ts` `seedRolesMatrix`:
    ```typescript
    import type { drizzle } from 'drizzle-orm/postgres-js';
    import { sql } from 'drizzle-orm';

    export interface SeededCalendarFixtures {
      eventIds: Record<string, string>; // 'training' | 'tournament' | 'meeting' | 'stage' | 'eval_conversation' | 'medical' | 'training_recurring' | 'training_recurring_with_exception' | 'overlapping_pair_a' | 'overlapping_pair_b'
      participantsByEvent: Record<string, string[]>;
    }

    /**
     * Seed canonical calendar fixtures on top of seedRolesMatrix output.
     * Creates 1 event per type with TD creator, 1 recurring weekly training,
     * 1 exception (cancelled occurrence), 1 overlapping pair for conflict testing.
     *
     * MUST be called AFTER seedRolesMatrix so the user ids exist.
     */
    export async function seedCalendarFixtures(
      db: ReturnType<typeof drizzle>,
      seededUsers: { technical_director: string; trainer: string; player: string; academy_manager: string; parent: string; sparring_partner: string; medical_staff: string },
    ): Promise<SeededCalendarFixtures> {
      // TODO Wave 5: implement INSERT statements per RESEARCH §Pattern 2 + 03-PATTERNS.md §calendar-seed.ts.
      throw new Error('seedCalendarFixtures: Wave 1+ schema must land before implementation');
    }
    ```

    ---

    `tests/unit/lookup-codes.test.ts` (MODIFY existing file) — extend with 6 event_type codes per UI3-D11:

    Read the current file, find the array of expected lookup codes, and APPEND:
    ```typescript
    // Phase 3 — event_type codes (UI3-D11, D-47)
    'event_type_training',
    'event_type_tournament',
    'event_type_meeting',
    'event_type_stage',
    'event_type_eval_conversation',
    'event_type_medical',
    ```
    in the appropriate constant or expectations list. If the file uses a `EXPECTED_CODES` map keyed by table name, add a new entry `event_type: [...]` with the 6 codes. Do NOT rewrite the file; only add.

    ---

    `tests/unit/schema-locale.test.ts` (MODIFY existing file) — extend with Phase 3 namespaces:

    Append to the existing `EXPECTED_NAMESPACES` list (or equivalent):
    ```typescript
    // Phase 3 — calendar namespaces
    'calendar',
    'lookup.eventType',
    'errors.calendar',
    ```
    Or, if the file iterates message-key trees per phase, add a new describe block:
    ```typescript
    describe.todo('Phase 3 — calendar namespace coverage (nl/en/fr)');
    ```

    ---

    `tests/unit/migration-format.test.ts` (MODIFY existing file) — extend with 4 new migrations + rollback companions per MIG-05:

    Find the existing list of expected migrations (e.g. `MIGRATIONS = ['0000_initial', '0001_medical_isolated', ...]`) and append:
    ```typescript
    '0009_phase3_calendar_base_lookup_participants_exceptions',
    '0010_phase3_calendar_extension_tables',
    '0011_phase3_calendar_rls_policies',
    '0012_phase3_event_type_seed',
    ```
    For each appended migration the existing rollback-md check loop will assert `*.rollback.md` exists with Risk/Procedure/Verification markers. Do NOT rewrite the file's loop logic; the appended entries inherit the test contract.

    ---

    All test files MUST:
    - Live at the exact paths listed in `files`.
    - Have JSDoc header that includes "RED scaffold — Phase 3 Wave 0".
    - Reference: D-XX / RESEARCH §X / VALIDATION line N.
    - Pass `pnpm test --reporter dot` syntactically (todo/skip blocks don't fail).
    - The 4 e2e specs MUST NOT actually run a real Playwright session yet — `test.todo()` and `test.skip(true, '...')` keep them off the runner.
  </action>
  <verify>
    <automated>test -f tests/unit/rrule.test.ts && test -f tests/unit/color-tokens.test.ts && test -f tests/unit/calendar-schemas.test.ts && test -f tests/integration/calendar-rls.test.ts && test -f tests/integration/calendar-rrule-horizon.test.ts && test -f tests/integration/calendar-exceptions.test.ts && test -f tests/integration/calendar-conflicts.test.ts && test -f tests/integration/calendar-audit.test.ts && test -f tests/integration/calendar-cascade.test.ts && test -f tests/integration/calendar-decline.test.ts && test -f tests/integration/calendar-perf.test.ts && test -f tests/integration/calendar-filter-options.test.ts && test -f tests/rls/calendar-direct-query.test.ts && test -f tests/e2e/calendar-week-view.spec.ts && test -f tests/e2e/calendar-create-event.spec.ts && test -f tests/e2e/calendar-mobile.spec.ts && test -f tests/e2e/calendar-drag.spec.ts && test -f tests/fixtures/calendar-seed.ts && pnpm test -- --run --reporter dot 2>&1 | head -100 && grep -c 'event_type_training\|event_type_tournament\|event_type_meeting\|event_type_stage\|event_type_eval_conversation\|event_type_medical' tests/unit/lookup-codes.test.ts | awk '{exit ($1 >= 6 ? 0 : 1)}' && grep -q '0009_phase3_calendar' tests/unit/migration-format.test.ts && grep -q '0012_phase3_event_type_seed' tests/unit/migration-format.test.ts</automated>
  </verify>
  <done>All 20 test files exist at the canonical paths; `pnpm test --run` exits with vitest "PASS" or "skip" for todo-only files (no syntax errors); `tests/fixtures/calendar-seed.ts` exports `seedCalendarFixtures` (still throws Wave-1-pending). Three modified test files have the new entries appended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node binary cache → repo | npm install pulls 8 new packages from registry — supply-chain risk window |
| Local dev → committed package.json | shadcn primitives are vendor-copied source; auditable post-install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-DEP-SUPPLY | T | FullCalendar 6.1.20 + rrule 2.8.1 + 7 plugins | accept | Verified MIT/BSD per RESEARCH; npm-tracked; existing Phase 1 npm audit gate covers this install. No new env vars or runtime privileges introduced. |
| T-03-02-DEP-PIN | T | package.json semver caret `^6.1.20` | mitigate | pnpm-lock.yaml pins the exact resolved version per Phase 1 lock-file discipline; CI must re-run with frozen lockfile. |
| T-03-03-TEST-FALSE-POSITIVE | V11 | RED test files | mitigate | Every `it.todo()` / `test.todo()` is unimplemented — vitest reports them as pending, not pass; Wave 5 converts each todo to a real assertion. The `seedCalendarFixtures` helper throws on call so Wave 1+ tests cannot silently green without implementation. |
</threat_model>

<verification>
- `pnpm typecheck` passes after Task 1.
- `pnpm test -- --run --reporter dot` runs to completion without syntax errors after Task 2.
- `grep` count of `event_type_*` codes in `tests/unit/lookup-codes.test.ts` ≥ 6.
- `migration-format.test.ts` references the 4 new Phase 3 migrations.
</verification>

<success_criteria>
- All 8 npm deps installed; all 6 shadcn primitives present at canonical paths.
- 20 RED test files exist with vitest/Playwright-compatible syntax; `pnpm test --run` does not error.
- `seedCalendarFixtures` exported (throws — Wave 1+ schema dependency).
- 3 modified test files extended (lookup-codes, schema-locale, migration-format) — appended entries, not rewritten.
</success_criteria>

<output>
After completion, create `.planning/phases/03-kalender/03-01-SUMMARY.md` documenting:
- All 8 npm packages with their resolved exact version from pnpm-lock.yaml
- All 6 shadcn primitive file paths
- The 20 test file paths (RED) + 1 fixture path
- Confirmation that `pnpm typecheck` and `pnpm test --run` both succeed
</output>
