---
phase: 04-kerndomein
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/unit/rrule-split.test.ts
  - tests/unit/quality-score-range.test.ts
  - tests/unit/match-derived-won.test.ts
  - tests/unit/ranking-xor.test.ts
  - tests/unit/entered-by-derivation.test.ts
  - tests/unit/outcome-level-seed.test.ts
  - tests/unit/rrule-byday.test.ts
  - tests/unit/idempotency-middleware.test.ts
  - tests/unit/i18n-catalog-completeness.test.ts
  - tests/unit/migration-format.test.ts
  - tests/integration/rbac-matrix-phase4.test.ts
  - tests/integration/14d-walls.test.ts
  - tests/integration/tournament-atomic-entry.test.ts
  - tests/integration/tournament-enter-result.test.ts
  - tests/integration/tournament-entry-window.test.ts
  - tests/integration/tournament-backfill-rbac.test.ts
  - tests/integration/tournament-td-overwrite.test.ts
  - tests/integration/tournament-create-rbac.test.ts
  - tests/integration/rrule-edit-scopes.test.ts
  - tests/integration/rls-academy-wide-result-visibility.test.ts
  - tests/integration/pg-cron-nudge-jobs.test.ts
  - tests/integration/training-mark-attendance.test.ts
  - tests/integration/session-participants-rls.test.ts
  - tests/integration/session-participants-occurrence.test.ts
  - tests/integration/sparring-partner-rls.test.ts
  - tests/integration/ranking-xor-constraint.test.ts
  - tests/integration/ranking-entry-rbac.test.ts
  - tests/integration/age-category-snapshot.test.ts
  - tests/integration/training-medical-conflict.test.ts
  - tests/integration/attendance-medical-default.test.ts
  - tests/integration/match-result-unique.test.ts
  - tests/integration/idempotency-tournament.test.ts
  - tests/integration/idempotency-ranking.test.ts
  - tests/integration/phase4-audit.test.ts
  - tests/fixtures/phase4-seed.ts
  - messages/nl.json
  - messages/en.json
  - messages/fr.json
autonomous: true
requirements:
  - TRAIN-01
  - TRAIN-02
  - TRAIN-03
  - TRAIN-04
  - TRAIN-05
  - TRAIN-06
  - TOURN-01
  - TOURN-02
  - TOURN-03
  - TOURN-04
  - TOURN-05
  - TOURN-06
  - RANK-01
  - RANK-02
  - RANK-03
  - RANK-04
  - RANK-05
  - RANK-06
  - RANK-07
  - DOM-RESULT-01
  - DOM-RESULT-02
  - DOM-RESULT-03
  - DOM-RESULT-04
  - DOM-RANK-01

must_haves:
  truths:
    - "REQUIREMENTS.md DOM-RESULT-01 marked SUPERSEDED-BY D-74 (single 14d window, no 48h sub-clock)"
    - "REQUIREMENTS.md DOM-RESULT-03 marked SUPERSEDED-BY D-76 (no edit-history table; audit_log JSONB is forensic recovery)"
    - "REQUIREMENTS.md DOM-RESULT-04 marked SUPERSEDED-BY D-77 (no status lifecycle)"
    - "REQUIREMENTS.md TOURN-04 marked PARTIALLY-SUPERSEDED-BY D-81 (gewonnen/verloren toggle derived from sets_won > sets_lost)"
    - "Wave 0 RED test files exist as failing skeletons for every entry in 04-VALIDATION.md §Wave 0 Requirements"
    - "i18n keyspaces training.*, tournament.*, ranking.*, nudge.*, errors.training.*, errors.tournament.*, errors.ranking.*, lookup.outcomeLevel.*, lookup.belgiumClassification.*, lookup.rankingType.*, lookup.trainingType.*, lookup.organisation.*, lookup.tournamentType.*, lookup.tournamentRound.*, lookup.rrule.bydayPreview have at least placeholder entries in all three locales (nl/en/fr)"
    - "tests/unit/i18n-catalog-completeness.test.ts asserts nl.json, en.json and fr.json have identical key sets and zero MISSING_TRANSLATION markers"
    - "tests/unit/migration-format.test.ts extended to include the seven Phase 4 migrations 0014..0020 in its rollback-companion verification list"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Source-of-truth requirement file with Phase 4 supersedes annotations"
      contains: "SUPERSEDED-BY D-74"
    - path: "tests/fixtures/phase4-seed.ts"
      provides: "Phase 4 test fixture seeder extending tests/fixtures/calendar-seed.ts with session_participants, tournament_results, match_results, ranking_entries, belgium_classification seed rows + sparring_partner role fixture"
      exports: ["seedPhase4"]
    - path: "tests/unit/rrule-split.test.ts"
      provides: "RED test stub for D-84 splitRRule() math"
      contains: "describe('splitRRule"
    - path: "tests/integration/14d-walls.test.ts"
      provides: "RED test stub for D-64 trainer wall + D-71 player wall boundary cases (day 13, 14 exact, 14d+1s, 15)"
      contains: "FOURTEEN_DAYS"
    - path: "tests/integration/tournament-atomic-entry.test.ts"
      provides: "RED test stub asserting D-69 atomicity (outcome + matches[] in single tx; rollback on partial failure)"
      contains: "rollback"
    - path: "tests/integration/rrule-edit-scopes.test.ts"
      provides: "RED test stub for D-84 three scopes (single / this_and_future / all_in_series) covering both training_sessions and meetings"
      contains: "all_in_series"
    - path: "tests/integration/rls-academy-wide-result-visibility.test.ts"
      provides: "RED test stub for D-78 5-branch UNION (TD all + own + trainer/manager + parent + same-academy player)"
      contains: "Branch 5"
    - path: "messages/nl.json"
      provides: "Dutch i18n catalog extended with Phase 4 keyspaces and stable key inventory"
      contains: "errors.training.scoreWindowExpired"
    - path: "messages/en.json"
      provides: "English i18n catalog extended with the same key set as nl.json"
      contains: "errors.training.scoreWindowExpired"
    - path: "messages/fr.json"
      provides: "French i18n catalog extended with the same key set as nl.json"
      contains: "errors.training.scoreWindowExpired"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/04-kerndomein/04-CONTEXT.md"
      via: "SUPERSEDED-BY D-XX inline pointers"
      pattern: "SUPERSEDED-BY D-7[4-7]|PARTIALLY-SUPERSEDED-BY D-81"
    - from: "tests/fixtures/phase4-seed.ts"
      to: "tests/fixtures/calendar-seed.ts"
      via: "import + extend"
      pattern: "from.*calendar-seed"
    - from: "messages/en.json"
      to: "messages/nl.json"
      via: "matching key set asserted by tests/unit/i18n-catalog-completeness.test.ts"
      pattern: "errors\\.training\\.scoreWindowExpired"
---

<objective>
Plan 04-01 owns the **Wave 0 RED scaffolding** for Phase 4: every test file from `04-VALIDATION.md` written as a failing skeleton; the REQUIREMENTS.md supersedes annotations done early so Wave 1 migrations are written against an honest spec; the i18n keyspaces in nl/en/fr seeded with at minimum placeholder values so Wave 2/3 routers and Wave 3 UI components can compile and run.

Purpose: prevent the verification-state-mismatch failure mode where downstream waves ship green builds against a stale REQUIREMENTS.md or against tests that don't exist yet. The seven Phase 4 migrations 0014..0020 also get added to the `tests/unit/migration-format.test.ts` rollback list so each migration's rollback companion is enforced.

Output: ~30 test files (mostly RED), three updated i18n catalogs, REQUIREMENTS.md updates, extended migration-format test, and `tests/fixtures/phase4-seed.ts` ready for Wave 2 consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-kerndomein/04-CONTEXT.md
@.planning/phases/04-kerndomein/04-RESEARCH.md
@.planning/phases/04-kerndomein/04-PATTERNS.md
@.planning/phases/04-kerndomein/04-VALIDATION.md
@.planning/phases/04-kerndomein/04-UI-SPEC.md
@./CLAUDE.md

<interfaces>
<!-- Key analogs the executor uses verbatim. Do NOT re-derive these in the tasks. -->

From tests/fixtures/calendar-seed.ts (Phase 3 — the seed helper Phase 4 extends):
```typescript
// Exports a seedCalendar(db, opts) helper that plants users (one per role), academy_memberships,
// parent_child_links, and a handful of calendar_events fixtures. Phase 4's seedPhase4 imports
// this and additionally plants session_participants, tournament_results, match_results,
// ranking_entries, belgium_classification rows, and a sparring_partner test user.
```

From tests/unit/migration-format.test.ts (the rollback-companion enforcer):
```typescript
// Iterates over a hard-coded MIGRATIONS array, asserts each .sql file has a matching
// .rollback.md companion containing the three headers: '**Risk:**', '**Procedure:**',
// '**Verification:**'. Phase 4 must add migrations 0014..0020 to the MIGRATIONS array.
```

From messages/nl.json (Phase 3 head — locale catalog structure):
```json
{
  "auth": { ... },
  "calendar": { ... },
  "errors": { "calendar": { ... } },
  "lookup": { "eventType": { ... } }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md supersedes + Wave 0 i18n placeholders + migration-format extension</name>
  <files>
    .planning/REQUIREMENTS.md,
    tests/unit/migration-format.test.ts,
    tests/unit/i18n-catalog-completeness.test.ts,
    messages/nl.json,
    messages/en.json,
    messages/fr.json
  </files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/phases/04-kerndomein/04-CONTEXT.md (especially §requirements_supersedes),
    .planning/phases/04-kerndomein/04-UI-SPEC.md §"Multilingual Copy" §"Keyspace inventory" + §"Critical copy samples",
    tests/unit/migration-format.test.ts,
    messages/nl.json (read head to see existing top-level shape),
    messages/en.json,
    messages/fr.json
  </read_first>
  <action>
    **Step 1 — REQUIREMENTS.md supersedes annotations (per `<requirements_supersedes>` in 04-CONTEXT.md):**

    Edit `.planning/REQUIREMENTS.md` and append the following annotations to the FOUR listed line items in the `### DOM — Domain-Specific Workflows (Refinements)` and `### TOURN — Tournaments & Results` sections. Leave the original requirement text intact and append the marker; do NOT delete the requirement IDs (the planner's coverage gate still counts them).

    Exact edits — for each bullet below, locate the line and append the marker AFTER the existing sentence (separated by ` **[SUPERSEDED-BY ...]**` so the marker is on the same logical line in markdown):

    | Line in REQUIREMENTS.md (locate by ID prefix) | Append text |
    |---|---|
    | `- [ ] **DOM-RESULT-01**: Player can edit own tournament results within 48h of entry; later edits require TD approval` | ` **[SUPERSEDED-BY D-74 — see .planning/phases/04-kerndomein/04-CONTEXT.md §B. No 48h sub-clock; single 14d player window per D-71. Coverage carried by D-71+D-73+D-74.]**` |
    | `- [ ] **DOM-RESULT-03**: Edit history table records every change to a result: …` | ` **[SUPERSEDED-BY D-76 — see .planning/phases/04-kerndomein/04-CONTEXT.md §B. No dedicated edit-history table; GDPR-04 audit_log JSONB snapshot is the forensic recovery path.]**` |
    | `- [ ] **DOM-RESULT-04**: Result lifecycle states: \`draft → confirmed → published\`; only \`confirmed\` and \`published\` results count in ambition vs. actuals comparison` | ` **[SUPERSEDED-BY D-77 — see .planning/phases/04-kerndomein/04-CONTEXT.md §B. No status lifecycle; every saved row counts in Phase 5 ambition comparison.]**` |
    | `- [ ] **TOURN-04**: Per-match result (level 2): ronde …, gewonnen/verloren (toggle), optionele video link` | ` **[PARTIALLY-SUPERSEDED-BY D-81 — see .planning/phases/04-kerndomein/04-CONTEXT.md §B. Set tally schema: sets_won/sets_lost smallint 0..4; gewonnen/verloren derived at query time from sets_won > sets_lost; no boolean column.]**` |

    Use the Edit tool four times — one Edit per requirement line — to avoid clobbering surrounding context. Verify each Edit by reading the surrounding 5 lines back.

    **Step 2 — Extend `tests/unit/migration-format.test.ts`:**

    Read the current MIGRATIONS array (it lists 0000..0013). Add the seven Phase 4 migration prefixes in numeric order:
    ```
    '0014_phase4_session_participants_and_sparring_junction',
    '0015_phase4_tournament_results_and_match_results',
    '0016_phase4_rankings_and_belgium_classification',
    '0017_phase4_lookup_seeds',
    '0018_phase4_rls_helpers_and_sparring_branch',
    '0019_phase4_pg_cron_nudges',
    '0020_phase4_system_inbox',
    ```

    The test should remain RED at this point (the .sql + .rollback.md files do not exist yet — they ship in Plan 04-02). Document that expectation in a top-of-file comment.

    **Step 3 — Create `tests/unit/i18n-catalog-completeness.test.ts` (new file):**

    Write a vitest test that:
    1. Reads `messages/nl.json`, `messages/en.json`, `messages/fr.json`.
    2. Flattens each into a Set of dot-joined key paths (e.g., `training.score.title`, `errors.training.scoreWindowExpired`).
    3. Asserts `nl.keys() == en.keys() == fr.keys()` using set-difference; emits the missing keys per-side on failure.
    4. Asserts that no value in any of the three files equals the literal string `[MISSING_TRANSLATION]`, `TODO`, or `__TODO__`.

    Use plain `fs.readFileSync` synchronously inside the test body — this is a static-file invariant, no DB needed.

    **Step 4 — Phase 4 i18n keyspace seeding into nl/en/fr:**

    Append the Phase 4 namespaces under each top-level key in `messages/nl.json`, `messages/en.json`, `messages/fr.json`. Use the canonical Dutch values from 04-UI-SPEC.md §"Critical copy samples" verbatim; use the canonical English and French values from the same table verbatim. For keys not in the §"Critical copy samples" table, use the Dutch text as a placeholder for nl AND copy the same Dutch text into en/fr — these are placeholder rows that Plan 04-08 (UI) will refine. **Crucially**, the SAME KEY SET must exist in all three files so `i18n-catalog-completeness.test.ts` passes from Wave 0 onwards.

    Required keys (minimum — every keyspace from 04-UI-SPEC.md §"Keyspace inventory" gets at least one entry):

    Under `training`:
      - `training.score.title`
      - `training.score.metadataDate`, `metadataTrainer`, `metadataType`, `metadataOrg`
      - `training.score.column.player`, `column.attendance`, `column.score`, `column.feedback`
      - `training.score.attendance.present`, `attendance.absent`, `attendance.absentMedical`, `attendance.pending`
      - `training.score.stars.aria`, `stars.clear`
      - `training.score.feedback.placeholder`, `feedback.charCount`
      - `training.score.save`, `saveSuccess`, `saveError`
      - `training.teScoren.heading`, `column.date`, `column.trainer`, `column.type`, `column.progress`, `progressLabel`, `scoreNow`, `empty`
      - `training.chip.needsScoringTooltip`
      - `training.eventSheet.openScoring`

    Under `tournament`:
      - `tournament.list.title`, `tournament.detail.title`, `tournament.create.title`, `tournament.participants.heading`, `tournament.result.title`, `tournament.result.section.outcome`, `result.section.matches`, `result.field.outcome`, `result.field.ageCategory`, `result.addMatch`, `result.save`, `result.overwriteBadge`, `result.backfillBadge`
      - `tournament.matchResults.column.round`, `column.opponent`, `column.opponentRanking`, `column.date`, `column.setsWon`, `column.setsLost`, `column.result`, `column.video`, `won`, `lost`, `draw`, `removeRow.aria`
      - `tournament.leaderboard.heading`
      - `tournament.chip.needsResultTooltip`
      - `tournament.eventSheet.enterResult`, `eventSheet.viewResult`
      - `tournament.pendingWidget.heading`, `pendingWidget.empty`

    Under `ranking`:
      - `ranking.title`
      - `ranking.types.international.senior_world`, `youth_world`, `senior_european`, `youth_european`
      - `ranking.types.belgium`
      - `ranking.range.1m`, `6m`, `1y`, `2y`, `all`
      - `ranking.chart.yAxisLabel`, `tooltip.source.manual`, `tooltip.source.federationOfficial`
      - `ranking.empty.heading`, `empty.body`, `empty.cta`
      - `ranking.belgium.empty.heading`, `belgium.empty.body`, `belgium.empty.cta`
      - `ranking.entry.title`, `field.type`, `field.date`, `field.value.international`, `field.value.belgium`, `disclaimer`, `save`
      - `ranking.entries.heading`

    Under `nudge`:
      - `nudge.trainerScore.day0to6`, `day7to9`, `day10to12`
      - `nudge.playerResult.day0to6`, `day7to9`, `day10to12`
      - `nudge.cta.scoreNow`, `cta.enterResult`, `cta.viewPending`
      - `nudge.inbox.trainerScoreBody`, `inbox.playerResultBody`

    Under `errors.training`:
      - `errors.training.scoreWindowExpired` — nl: "Het scoren-venster voor deze training is verstreken (14 dagen)." / en: "The scoring window for this training has expired (14 days)." / fr: "La fenêtre de saisie pour cet entraînement est expirée (14 jours)."
      - `errors.training.scoreRange` — nl: "De score moet tussen 1 en 10 liggen." / en: "The score must be between 1 and 10." / fr: "Le score doit être compris entre 1 et 10."
      - `errors.training.qualityScoreOutOfRange`

    Under `errors.tournament`:
      - `errors.tournament.entryWindowExpired` — verbatim from 04-UI-SPEC.md sample table
      - `errors.tournament.atLeastOneMatchRequired` — nl: "Voeg minstens één wedstrijd toe." / en: "Add at least one match." / fr: "Ajoutez au moins un match."
      - `errors.tournament.outcomeRequired`, `errors.tournament.setRange`, `errors.tournament.videoLink`, `errors.tournament.duplicateMatch`

    Under `errors.ranking`:
      - `errors.ranking.unknownType`, `expectedNumeric`, `expectedClassification`, `bothValuesPopulated`, `valueRequired`, `classificationRequired`, `dateRequired`, `mustBePositive`

    Under `errors.calendar`:
      - `errors.calendar.rruleBydayRequired` — extends Phase 3 errors.calendar.* keyspace.

    Under `lookup.outcomeLevel` — 9 codes verbatim from 04-UI-SPEC.md §"Lookup outcomeLevel":
      - `outcome_winner`, `outcome_finalist`, `outcome_last_4`, `outcome_last_8`, `outcome_last_16`, `outcome_last_32`, `outcome_last_64`, `outcome_last_128`, `outcome_group_stage`

    Under `lookup.belgiumClassification` — at minimum these codes (the full A1..A50 set is seeded in Plan 04-02; here we declare label entries for the most common ones; per I18N-06 codes ARE labels — same value in all 3 locales):
      - `A1`, `A2`, `A12`, `A50` (and you may add A3..A11 and A13..A49 as identity mappings: `"A37": "A37"` etc. for completeness)
      - `B0`, `B2`, `B4`, `B6`
      - `C0`, `C2`, `C4`, `C6`
      - `D0`, `D2`, `D4`, `D6`
      - `E0`, `E2`, `E4`, `E6`
      - `NC`

    Under `lookup.rankingType` — 5 codes verbatim:
      - `ranking_senior_world`, `ranking_youth_world`, `ranking_senior_european`, `ranking_youth_european`, `ranking_belgium`

    Under `lookup.trainingType` — placeholder labels for the 4 codes from CONTEXT D-60..D-68: `training_type_group`, `training_type_individual`, `training_type_physical`, `training_type_mental`.

    Under `lookup.organisation` — 6 codes: `org_private`, `org_kbttb`, `org_topsportschool`, `org_academie`, `org_provinciaal`, `org_club`.

    Under `lookup.tournamentType` — 7 codes: `tournament_wtt`, `tournament_wtt_star`, `tournament_ettu`, `tournament_ejk`, `tournament_wk`, `tournament_international`, `tournament_belgium`.

    Under `lookup.tournamentRound` — 10 codes verbatim from 04-UI-SPEC.md §"Lookup tournamentRound": `round_final`, `round_semi`, `round_quarter`, `round_eighth`, `round_sixteenth`, `round_thirty_second`, `round_sixty_fourth`, `round_one_twenty_eighth`, `round_group_stage`, `round_other`.

    Under `lookup.rrule`:
      - `lookup.rrule.bydayPreview` — nl: "Bv. {days}" / en: "E.g., {days}" / fr: "Ex. {days}"
      - `lookup.rrule.days.mo`, `days.tu`, `days.we`, `days.th`, `days.fr`, `days.sa`, `days.su` — locale-aware day abbreviations per UI4-D19 (nl: Ma/Di/Wo/Do/Vr/Za/Zo; en: Mon/Tue/Wed/Thu/Fri/Sat/Sun; fr: Lu/Ma/Me/Je/Ve/Sa/Di)

    Under `dashboard`:
      - `dashboard.title`

    **Order rule:** preserve alphabetical sort within each top-level namespace where possible; preserve the EXACT order of existing keys (don't reformat the entire file).

    **Step 5 — Verify catalog completeness locally before commit:**

    Run `pnpm test -- tests/unit/i18n-catalog-completeness.test.ts --run` and ensure it PASSES (all three files have the same key set). If it fails because a key is missing in one file, add the missing key as `"<placeholder>": "<dutch-value>"` to fix.
  </action>
  <verify>
    <automated>grep -q "SUPERSEDED-BY D-74" .planning/REQUIREMENTS.md && grep -q "SUPERSEDED-BY D-76" .planning/REQUIREMENTS.md && grep -q "SUPERSEDED-BY D-77" .planning/REQUIREMENTS.md && grep -q "PARTIALLY-SUPERSEDED-BY D-81" .planning/REQUIREMENTS.md && grep -q "0014_phase4_session_participants_and_sparring_junction" tests/unit/migration-format.test.ts && grep -q "0020_phase4_system_inbox" tests/unit/migration-format.test.ts && test -f tests/unit/i18n-catalog-completeness.test.ts && grep -q "errors.training.scoreWindowExpired" messages/nl.json && grep -q "errors.training.scoreWindowExpired" messages/en.json && grep -q "errors.training.scoreWindowExpired" messages/fr.json && grep -q "lookup.belgiumClassification" messages/nl.json && pnpm test -- tests/unit/i18n-catalog-completeness.test.ts --run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains all four SUPERSEDED-BY / PARTIALLY-SUPERSEDED-BY markers, each pointing to the controlling D-XX in 04-CONTEXT.md.
    - tests/unit/migration-format.test.ts MIGRATIONS array contains the seven Phase 4 entries `0014_*` through `0020_*` in numeric order.
    - tests/unit/i18n-catalog-completeness.test.ts EXISTS and PASSES (`pnpm test -- ... --run` exit 0) — proving nl/en/fr have the same key set and zero placeholder strings.
    - All three locale files contain `errors.training.scoreWindowExpired`, `errors.tournament.entryWindowExpired`, `lookup.outcomeLevel.outcome_winner`, `lookup.belgiumClassification.A1`, `lookup.rankingType.ranking_belgium`, `nudge.trainerScore.day10to12`, `ranking.chart.yAxisLabel` — verified via `grep -q`.
    - tests/unit/migration-format.test.ts itself remains RED (expected — the migration .sql files don't exist yet); no other previously-green test regresses (run `pnpm test -- tests/unit/ --run --reporter=dot` and verify that any new failure is ONLY in migration-format.test.ts).
  </acceptance_criteria>
  <done>REQUIREMENTS.md supersedes annotations committed; tests/unit/migration-format.test.ts extended for 0014..0020; i18n catalog completeness test green; all three locale files synchronized with Phase 4 keyspaces.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 RED test scaffolds (unit + integration) + tests/fixtures/phase4-seed.ts</name>
  <files>
    tests/fixtures/phase4-seed.ts,
    tests/unit/rrule-split.test.ts,
    tests/unit/quality-score-range.test.ts,
    tests/unit/match-derived-won.test.ts,
    tests/unit/ranking-xor.test.ts,
    tests/unit/entered-by-derivation.test.ts,
    tests/unit/outcome-level-seed.test.ts,
    tests/unit/rrule-byday.test.ts,
    tests/unit/idempotency-middleware.test.ts,
    tests/integration/rbac-matrix-phase4.test.ts,
    tests/integration/14d-walls.test.ts,
    tests/integration/tournament-atomic-entry.test.ts,
    tests/integration/tournament-enter-result.test.ts,
    tests/integration/tournament-entry-window.test.ts,
    tests/integration/tournament-backfill-rbac.test.ts,
    tests/integration/tournament-td-overwrite.test.ts,
    tests/integration/tournament-create-rbac.test.ts,
    tests/integration/rrule-edit-scopes.test.ts,
    tests/integration/rls-academy-wide-result-visibility.test.ts,
    tests/integration/pg-cron-nudge-jobs.test.ts,
    tests/integration/training-mark-attendance.test.ts,
    tests/integration/session-participants-rls.test.ts,
    tests/integration/session-participants-occurrence.test.ts,
    tests/integration/sparring-partner-rls.test.ts,
    tests/integration/ranking-xor-constraint.test.ts,
    tests/integration/ranking-entry-rbac.test.ts,
    tests/integration/age-category-snapshot.test.ts,
    tests/integration/training-medical-conflict.test.ts,
    tests/integration/attendance-medical-default.test.ts,
    tests/integration/match-result-unique.test.ts,
    tests/integration/idempotency-tournament.test.ts,
    tests/integration/idempotency-ranking.test.ts,
    tests/integration/phase4-audit.test.ts
  </files>
  <read_first>
    tests/fixtures/calendar-seed.ts,
    tests/integration/calendar-rls.test.ts (analog for RLS test shape),
    tests/integration/calendar-rrule-horizon.test.ts (analog for boundary tests),
    tests/integration/calendar-exceptions.test.ts (analog for CHECK constraint violation expectations),
    tests/integration/calendar-audit.test.ts (analog for audit-log assertion shape),
    tests/integration/rbac-matrix.test.ts (canonical RBAC matrix probe shape),
    tests/unit/calendar-schemas.test.ts (analog for Zod safeParse / discriminated-union assertions),
    tests/unit/rrule.test.ts (analog for pure-function rrule test),
    .planning/phases/04-kerndomein/04-RESEARCH.md §"Validation Architecture" and §"Wave 0 Gaps",
    .planning/phases/04-kerndomein/04-VALIDATION.md §"Wave 0 Requirements",
    .planning/phases/04-kerndomein/04-CONTEXT.md (D-60..D-91 — every test names the decision it covers in its describe block)
  </read_first>
  <action>
    Create each test file from `04-VALIDATION.md §Wave 0 Requirements` (mirrored in `04-RESEARCH.md §Wave 0 Gaps`) as a **failing skeleton** following the canonical analog shape. Every file MUST:

    1. Open with a top-of-file comment block citing the decision ID(s) it covers, the analog test file used as template, and a `// RED: this test is intentionally failing until <plan-NN> ships <feature>.` line.
    2. Have at least one `describe` + one `it.skip` OR `it.todo` block. Where a deterministic shape exists (`safeParse` on a not-yet-existing schema, raw SQL constraint on a not-yet-existing table), prefer `it` over `it.skip` so the test is genuinely RED, not skipped. The orchestrator will see RED tests in CI from Wave 0 forward, which is the desired Nyquist signal.
    3. For integration tests: import the seed helper from `tests/fixtures/phase4-seed.ts` (created in this task) and call it inside a `beforeAll` setup.
    4. Use vitest test names that match the Wave 0 list verbatim (e.g., `describe('14-day score wall (D-64)', () => { it('rejects score entry on day 15 with errors.training.scoreWindowExpired', async () => { ... }); });`).

    **Sub-step 2a — Create `tests/fixtures/phase4-seed.ts` (the seed helper Wave 0 tests import):**

    ```typescript
    /**
     * tests/fixtures/phase4-seed.ts — Phase 4 test fixture seeder.
     *
     * Extends tests/fixtures/calendar-seed.ts (Phase 3) with the operational
     * tables Phase 4 ships:
     *   - session_participants (PK: event_id, occurrence_date, user_id)
     *   - session_sparring_partners (event_id, sparring_partner_id)
     *   - tournament_results (PK: tournament_event_id, player_user_id)
     *   - match_results
     *   - ranking_entries (XOR value)
     *   - belgium_classification lookup rows (subset)
     *
     * Also adds a sparring_partner test user (filling Phase 3 RLS placeholder).
     *
     * Each function below is idempotent (ON CONFLICT DO NOTHING) so tests can call
     * the seeder multiple times.
     *
     * NOTE: this file imports schema modules that don't exist yet (they ship in
     * Plan 04-02). Use TypeScript dynamic imports + try/catch to make it survive
     * Wave 0 typecheck. Wave 2 tests will import statically.
     */
    import type { Db } from '@/server/db/client';
    import { seedRolesMatrix } from '../helpers/seed';

    export interface Phase4SeedOptions {
      includeSparring?: boolean;
      includeBelgiumClassifications?: boolean;
      includeOutcomeLevels?: boolean;
    }

    export async function seedPhase4(db: Db, opts: Phase4SeedOptions = {}) {
      // Phase 1 fixtures: roles, academies, parent-child links
      const matrix = await seedRolesMatrix(db);

      // Phase 4 fixtures — populated by Wave 1 (schema ships in Plan 04-02);
      // until then, this is a thin stub that returns the roles matrix.
      // Tests that need session_participants / tournament_results rows
      // will write them inline within the test body until Wave 2.
      return { ...matrix };
    }
    ```

    The file must compile under Wave 0 (no schema imports yet). Wave 2 plans will extend it.

    **Sub-step 2b — Unit-test skeletons (8 files):**

    For each unit test in the list below, write the file with this exact top-of-file comment + one failing `it` block:

    - `tests/unit/rrule-split.test.ts` — covers D-84 splitRRule() math. Body: `it('truncates oldRrule UNTIL to splitDate - 1 day', () => { expect(() => require('@/lib/rrule').splitRRule).toBeDefined(); });` (will fail because splitRRule does not exist yet).
    - `tests/unit/quality-score-range.test.ts` — covers D-60 CHECK constraint + 5-star→2/4/6/8/10 mapping. Body: `it('5-star input maps to DB values 2/4/6/8/10', () => { expect(() => require('@/lib/quality-score').mapStarsToDb).toBeDefined(); });`
    - `tests/unit/match-derived-won.test.ts` — covers D-81 derivation. Body: `it('derives won=true when sets_won > sets_lost', () => { ... });`
    - `tests/unit/ranking-xor.test.ts` — covers D-86 split-column Zod discriminated union. Body: `it('rejects when both value.kind and value.code are populated', () => { ... });`
    - `tests/unit/entered-by-derivation.test.ts` — covers DOM-RESULT-02 attribution. Body: `it('derives entered_by from caller role', () => { ... });`
    - `tests/unit/outcome-level-seed.test.ts` — covers D-70 9 codes + sort_order. Body: `it('seed file 0017 declares exactly 9 outcome_level codes with sort_order 1..9', () => { ... });`
    - `tests/unit/rrule-byday.test.ts` — covers D-85 BYDAY+FREQ=WEEKLY serialization. Body: `it('serializes byweekday=[TU,TH] to BYDAY=TU,TH', () => { ... });`
    - `tests/unit/idempotency-middleware.test.ts` — covers VALID-08 wiring. Body: `it('short-circuits replay within 24h and returns cached responseBody', () => { ... });`

    **Sub-step 2c — Integration-test skeletons (23 files):**

    For each integration test, write the file with this shape:
    ```typescript
    /**
     * tests/integration/<name>.test.ts — covers <D-XX, REQ-YY>.
     * Analog: tests/integration/<analog>.test.ts.
     * RED: this test is intentionally failing until Plan 04-<NN> ships <feature>.
     */
    import { describe, it, beforeAll, expect } from 'vitest';
    import { db } from '@/server/db/client';
    import { seedPhase4 } from '../fixtures/phase4-seed';

    describe('<feature> (D-XX)', () => {
      beforeAll(async () => { await seedPhase4(db); });
      it('<expected behavior>', async () => {
        expect.fail('Not implemented: requires Plan 04-<NN>');
      });
    });
    ```

    Files to create (each with a focused `describe` block; analog reference taken from `04-PATTERNS.md §"Test files"`):

    | File | D-XX covered | Plan ref | Analog |
    |---|---|---|---|
    | `tests/integration/rbac-matrix-phase4.test.ts` | All RBAC | 04-03..06 | rbac-matrix.test.ts |
    | `tests/integration/14d-walls.test.ts` | D-64, D-71 | 04-03, 04-04 | calendar-rrule-horizon.test.ts |
    | `tests/integration/tournament-atomic-entry.test.ts` | D-69, D-80 | 04-04 | age-category-history.test.ts |
    | `tests/integration/tournament-enter-result.test.ts` | D-69 happy path | 04-04 | calendar-conflicts.test.ts |
    | `tests/integration/tournament-entry-window.test.ts` | D-71 | 04-04 | calendar-rrule-horizon.test.ts |
    | `tests/integration/tournament-backfill-rbac.test.ts` | D-73 | 04-04 | rbac-matrix.test.ts |
    | `tests/integration/tournament-td-overwrite.test.ts` | D-75 | 04-04 | calendar-audit.test.ts |
    | `tests/integration/tournament-create-rbac.test.ts` | D-79 | 04-04 | rbac-matrix.test.ts |
    | `tests/integration/rrule-edit-scopes.test.ts` | D-84 (all 3 scopes) | 04-06 | calendar-exceptions.test.ts |
    | `tests/integration/rls-academy-wide-result-visibility.test.ts` | D-78 5-branch UNION | 04-04 | calendar-rls.test.ts |
    | `tests/integration/pg-cron-nudge-jobs.test.ts` | D-67 ch2, D-72 ch2 | 04-07 | (no analog — see RESEARCH §Pitfall 2) |
    | `tests/integration/training-mark-attendance.test.ts` | D-62 + Pitfall 6 ON CONFLICT | 04-03 | age-category-history.test.ts |
    | `tests/integration/session-participants-rls.test.ts` | D-61 | 04-03 | calendar-rls.test.ts |
    | `tests/integration/session-participants-occurrence.test.ts` | D-82 | 04-02, 04-03 | calendar-exceptions.test.ts |
    | `tests/integration/sparring-partner-rls.test.ts` | D-63 + Branch 6 | 04-06 | calendar-rls.test.ts |
    | `tests/integration/ranking-xor-constraint.test.ts` | D-86 DB-level XOR | 04-05 | calendar-exceptions.test.ts |
    | `tests/integration/ranking-entry-rbac.test.ts` | D-89 | 04-05 | rbac-matrix.test.ts |
    | `tests/integration/age-category-snapshot.test.ts` | DOM-CAT-02 | 04-04 | age-category-history.test.ts |
    | `tests/integration/training-medical-conflict.test.ts` | DOM-MED-CONFLICT-01 | 04-03 | calendar-conflicts.test.ts |
    | `tests/integration/attendance-medical-default.test.ts` | DOM-MED-CONFLICT-02 | 04-03 | calendar-conflicts.test.ts |
    | `tests/integration/match-result-unique.test.ts` | VALID-07 | 04-04 | calendar-exceptions.test.ts |
    | `tests/integration/idempotency-tournament.test.ts` | VALID-08 | 04-04 | (no analog — see RESEARCH §Pitfall 5) |
    | `tests/integration/idempotency-ranking.test.ts` | VALID-08 | 04-05 | (no analog) |
    | `tests/integration/phase4-audit.test.ts` | GDPR-04 (all Phase 4 audit codes) | 04-03..05 | calendar-audit.test.ts |

    For `14d-walls.test.ts`, embed the four boundary cases verbatim:
    - day 13 → allowed
    - day 14 exact → allowed
    - day 14 + 1s → rejected (FORBIDDEN, message 'errors.training.scoreWindowExpired')
    - day 15 → rejected

    For `rls-academy-wide-result-visibility.test.ts`, embed the 5 branches as 5 separate `it` blocks:
    - Branch 1 (TD sees all)
    - Branch 2 (own results)
    - Branch 3 (trainer/academy_manager in player's academy)
    - Branch 4 (parent of subject minor)
    - Branch 5 (player sharing an academy with subject — net-new per D-78)

    For `rrule-edit-scopes.test.ts`, embed three `describe` blocks (scope=single / this_and_future / all_in_series) AND a fourth assertion that past `session_participants` rows are NEVER mutated regardless of scope (D-83 immutable past).

    For `phase4-audit.test.ts`, embed one `it` per audit code from 04-PATTERNS.md §"Cross-Cutting Patterns" §"2. Audit logging" (14 codes total: `training_attendance_marked`, `training_score_window_expired_attempt`, `tournament_result_entered`, `tournament_result_overwritten`, `tournament_entry_window_expired_attempt`, `tournament_created`, `tournament_participant_added`, `tournament_participant_removed`, `ranking_entry_added`, `ranking_entry_updated`, `calendar_event_recurring_split`, `calendar_event_recurring_updated_all`, `sparring_partner_attached`, `idempotency_replay`).

    **Sub-step 2d — Per-task VALIDATION.md update:**

    Edit `.planning/phases/04-kerndomein/04-VALIDATION.md` and fill the §"Per-Task Verification Map" table with rows for every TASK that ships in plans 04-01..04-09. One row per task — use the file format `04-NN-TT` for the task ID. Use the automated command from each task's `<verify>` block as the "Automated Command" cell. Set "File Exists" to `❌ W0` for any test created in this plan that's still RED, `✅` for files that already exist. Set "Status" to `⬜ pending`.

    Use the Edit tool on `04-VALIDATION.md`; preserve the existing header rows and §"Wave 0 Requirements" / §"Manual-Only Verifications" sections. Add ~25 new rows below the placeholder row.
  </action>
  <verify>
    <automated>test -f tests/fixtures/phase4-seed.ts && test -f tests/unit/rrule-split.test.ts && test -f tests/unit/quality-score-range.test.ts && test -f tests/unit/ranking-xor.test.ts && test -f tests/unit/rrule-byday.test.ts && test -f tests/unit/idempotency-middleware.test.ts && test -f tests/integration/14d-walls.test.ts && test -f tests/integration/tournament-atomic-entry.test.ts && test -f tests/integration/rrule-edit-scopes.test.ts && test -f tests/integration/rls-academy-wide-result-visibility.test.ts && test -f tests/integration/pg-cron-nudge-jobs.test.ts && test -f tests/integration/phase4-audit.test.ts && test -f tests/integration/sparring-partner-rls.test.ts && test -f tests/integration/session-participants-occurrence.test.ts && grep -q "describe.*D-64\|describe.*D-71" tests/integration/14d-walls.test.ts && grep -q "Branch 5" tests/integration/rls-academy-wide-result-visibility.test.ts && grep -q "all_in_series\|this_and_future" tests/integration/rrule-edit-scopes.test.ts && grep -q "tournament_result_overwritten\|idempotency_replay" tests/integration/phase4-audit.test.ts && grep -q "04-03-01\|04-04-01\|04-05-01" .planning/phases/04-kerndomein/04-VALIDATION.md && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 8 unit test files + 24 integration test files (32 total) exist with vitest skeleton bodies containing `describe` blocks naming the decision ID(s).
    - `tests/fixtures/phase4-seed.ts` exists, exports `seedPhase4(db, opts)`, and compiles under `pnpm typecheck` (no schema imports yet — uses dynamic imports or thin stub).
    - Test `14d-walls.test.ts` contains the four boundary case assertions (day 13, day 14, day 14+1s, day 15) as four distinct `it` blocks.
    - Test `rls-academy-wide-result-visibility.test.ts` contains 5 `it` blocks, one per UNION branch.
    - Test `rrule-edit-scopes.test.ts` contains describes for `single`, `this_and_future`, `all_in_series` + an immutable-past assertion (D-83).
    - Test `phase4-audit.test.ts` contains exactly 14 audit-code assertions (one per code in 04-PATTERNS.md §"Cross-Cutting Patterns" §2 — including `tournament_result_overwritten`, `idempotency_replay`, `calendar_event_recurring_split`).
    - `pnpm typecheck` exit 0 — no compilation errors from the new test files.
    - `pnpm test -- tests/unit/ --run --reporter=dot` runs to completion (some tests RED, that is expected).
    - `04-VALIDATION.md` §"Per-Task Verification Map" has rows for every task across plans 04-01..04-09 (validation map gate from `<quality_gate>`).
  </acceptance_criteria>
  <done>30+ Wave 0 RED tests scaffolded, seed helper compiles, validation map populated, `pnpm typecheck` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md → downstream planning artifacts | Source-of-truth document; missing supersedes annotation = false coverage signal in verification |
| Phase 4 Wave 0 tests → Phase 4 Wave 1+ implementation | Tests are the contract; missing test = uncaught regression |
| i18n catalogs (nl/en/fr) → user-facing UI | Missing locale entry surfaces as `[MISSING_TRANSLATION]` in production; per I18N-10, en/fr completeness blocks release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-VERIFICATION-STATE-DRIFT | T (Tampering — silent) | REQUIREMENTS.md | mitigate | Annotate the 4 superseded REQ-IDs with explicit SUPERSEDED-BY pointers IN THIS PLAN so downstream coverage gates count them honestly. Without this annotation, `requirements:` frontmatter list claims coverage for behaviors that no longer exist in the spec. |
| T-04-02-MISSING-LOCALE-FALLBACK | I (Information Disclosure — text leak) | messages/en.json, messages/fr.json | mitigate | i18n-catalog-completeness test asserts identical key sets and zero placeholder markers. en/fr currently mirror nl as placeholder; Plan 04-08 refines the en/fr copy under translator review per `project_i18n` memory. |
| T-04-03-MIGRATION-FORMAT-DRIFT | T | tests/unit/migration-format.test.ts | mitigate | The seven new migration prefixes 0014..0020 are added to the test's hard-coded list THIS PLAN so the test goes RED until Plan 04-02 ships the actual .sql + .rollback.md files. Test is forward-RED, not backward-blind. |
| T-04-04-WAVE-0-DRIFT-FROM-VALIDATION-MD | T | tests/* | mitigate | Every entry in 04-VALIDATION.md §"Wave 0 Requirements" gets a corresponding test file in this plan; the validation map per-task table is filled in this plan so downstream agents can verify completeness via the table. |
</threat_model>

<verification>
- REQUIREMENTS.md has 4 supersedes annotations grep-verified.
- 32 Wave 0 test files exist (`find tests -newer .planning/phases/04-kerndomein/04-CONTEXT.md -name '*.test.ts' | wc -l ≥ 30`).
- `pnpm test -- tests/unit/i18n-catalog-completeness.test.ts --run` exit 0.
- `pnpm typecheck` exit 0.
- All three messages/*.json contain the same top-level namespaces from 04-UI-SPEC.md §"Keyspace inventory".
- 04-VALIDATION.md per-task table populated.
</verification>

<success_criteria>
- REQUIREMENTS.md: 4 SUPERSEDED-BY annotations committed.
- 32 Wave 0 test skeletons exist + 1 seed helper.
- i18n catalogs synchronized across nl/en/fr; catalog-completeness test PASSES.
- migration-format test extended (still RED — that's the desired forward signal).
- `pnpm typecheck` PASSES.
- 04-VALIDATION.md per-task map populated for all 04-NN-TT task IDs across the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/04-kerndomein/04-01-SUMMARY.md` using the standard summary template.
</output>
